# Resume NoSpoilers: verify the whole scope and finish Gate B

Copy the prompt below into a later task.

```text
Continue NoSpoilers in /Users/augustusedem/Nospoilers. Finish the entire agreed Gate B, not just the most recently edited feature. Inspect the current repository and running application before relying on earlier conversation summaries. Do not give an unsupported completion percentage or infer completion from a passing test count.

Start by reading applicable AGENTS.md instructions and these project documents:
- docs/ENTERPRISE-READINESS-PLAN.md
- docs/GATE-B-COMPLETION-AUDIT.md
- docs/WEB-APP-ARCHITECTURE.md
- docs/MOCKUP-PRODUCTION-CONTRACT.md
- docs/IDENTITY-WORKSPACES.md
- docs/DATA-LIFECYCLE-POLICY.md
- docs/GATE-B-IMPLEMENTATION.md
- docs/GATE-B-WORKSPACE-EXCEPTIONS.md
- docs/GATE-B-WORKSPACE-CREDENTIALS.md
- docs/GATE-B-ALERT-CUTOVER.md
- docs/GATE-B-WEBSITE-CUTOVER.md
- docs/HANDOFF.md
Follow their relevant linked implementation records. Resolve historical or contradictory status notes against actual code and fresh evidence; update the authoritative ledger rather than adding another ambiguous progress claim.

Re-establish the complete B0–B8 requirements and inspect what is genuinely missing:
1. Identity/workspaces: creation, switcher, membership, invitations, permissions, connected-source ownership, shared billing and scan limits. Keep product membership independent from GitHub source access.
2. First Proof/setup: preserve the approved four-circle first-login journey; use real evidence-driven intermediate states and contextual connection diagnostics. Package-only customers must not need GitHub.
3. New Scan: GitHub selection, artifact upload, website ownership and proof verification. Verify validation, supported formats/limits, progress, cancellation, expiry, login return, queue, retry, permission and trial states. Signed-in scanning stays inside the product shell.
4. Coverage: inventory, owner/scope, connection health, successful check, cadence/next check and latest result. Verify connect/edit/pause/disconnect/reconnect across supported source types. Stale monitoring is not current clean evidence.
5. Releases/detail: stable history, filters, independently selectable preview, deep links, refresh/back/forward and immutable previous attempts. Verify queued/running/failed/inconclusive/findings/passed/passed-with-exceptions states and scope, unsupported checks, digest, timestamp, policy/engine revision and provenance.
6. Alerts/exceptions: Open/In progress/Resolved with separate ownership filtering, real evidence, assignment, response history, next action and related result. Workflow resolution does not erase exposure evidence. Exceptions require bounded scope, justification, expiry and independent approval where required, including mixed GitHub/independent workspaces and retained legacy history.
7. Proof: private by default, explicit redacted sharing preview, revocation, and valid/invalid/unrecognized issuer/revoked/mismatch outcomes. Verification must not create a scan job or reveal private evidence.
8. Overview: transition from First Proof using actual evidence; accurate linked counts for release decisions, open alerts and delayed coverage across independent and connected sources. Do not hide unknown scope behind a safety score.
9. Settings: real team, policy, retention, token and notification controls; permissions, save/error feedback and audit history. Preserve existing connected functionality while completing workspace integration.

For every requirement, record whether current evidence proves it complete, shows a concrete defect, or is still missing. Verify the actual user journeys against real APIs, not only component snapshots or mocks. Cover tenant isolation, revoked access, archived workspaces, subscription enforcement, stale responses, refresh/navigation and failure states. Do not create real external notifications, credentials, destructive data changes or paid resources merely to demonstrate a screen.

Use the approved master-flow HTML prototype and Mock 3 release-detail reference already in the repository. Preserve their clean dark presentation, restrained NoSpoilers red and agreed layouts while connecting real application behavior. Do not rebuild completed screens from scratch, introduce sample customer data or substitute a different product journey. Gate B must include usable states; keep broader Gate C visual/accessibility work separately identified rather than silently expanding or shrinking Gate B.

Keep the working checklist bounded by the original requirements. Fix actual failures and missing required flows; distinguish optional refinements from completion blockers. Complete meaningful end-to-end slices, then verify them. Preserve unrelated dirty changes. Use focused checks during implementation and a fresh full regression/build against unchanged source before completion. Never edit source or tests while that full regression is running. If a previous test/server process is still alive, inspect its actual handle before starting another.

Known checkpoint to revalidate, not assume current: on 6 September, independent exception policy/review was made accessible in both workspace shells, with GitHub connection policy preserved separately. A mixed-workspace worker test proved policy separation. Full regression was started with output at /private/tmp/nospoilers-gate-b-exceptions-full.json; check whether it actually completed and whether the source has changed since. Do not treat this checkpoint as completion of exceptions or Gate B.

Owner decisions remain in force:
- No Docker or Docker installation. Hosted worker/isolation validation is deferred; Railway may be assessed later.
- Do not activate Neon Auth, WorkOS or another provider without approval. Provider-neutral workspace migration is included now.
- No deployment, push, production migration or external provider activation without authorization.
- Five-day trial; no permanent free scanner. Public intake is a hook, while results and scan access remain gated as agreed.
- Disconnect preserves history. Destructive history/account deletion requires the explicit typed authorization described in the lifecycle policy.
- Do not label SSO/SCIM or other enterprise capabilities operational when they are not implemented.

Continue implementing until all Gate B acceptance requirements are genuinely met. If an external decision or permission is necessary, explain the exact blocker and continue other authorized work where possible. Keep architecture, completion ledger and relevant implementation records synchronized. At completion, report the requirements verified, fresh test/build/browser evidence and any separately deferred Gate A/C/D/E work. Do not declare the whole application launch-ready merely because Gate B is finished.
```
