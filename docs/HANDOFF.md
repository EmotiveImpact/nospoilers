# Next-agent handoff

## Current handoff — 22 September 2026

PR #44 was integrated and the active repository is on `main`. The provider-neutral identity
foundation is committed at `11f4a3a`; current documentation records the resulting decisions. Start
with [Status](STATUS.md), [Authentication, enterprise access and scan workers](AUTH-ENTERPRISE-AND-WORKERS.md),
[Bugs and fixes](../bugsandfixes.md), and the latest release-assurance build log. Do not resume the
old branch/PR continuation instructions below; they are historical.

Neon Managed Better Auth is selected for ordinary accounts but is not enabled because the fresh
Marketplace Neon account still needs owner email verification. GitHub remains a separate source
connector. WorkOS is a later enterprise SSO/SCIM adapter. Railway and a fresh digest-pinned Vercel
Sandbox per scan have passed a live clean probe and production preflight. The hosted customer journey,
live hostile-input/egress/interruption checks, real notification delivery, GitHub Actions account
recovery and native accessibility acceptance remain open. Stripe remains owner-deferred.

Final identity-source verification passed **1,660/1,660 tests across 271 files**, typecheck,
frontend/API builds, lint with existing warnings and diff check. Do not call the product publicly
production-ready from that result alone.

## Current handoff — 8 September

**Gate B is complete.** This paragraph supersedes the earlier active-status checkpoint below. Final regression38375 passes986/198, build27665 passes, lint exits0 with warnings. No verification remains running. See the closure at the top of `GATE-B-COMPLETION-AUDIT.md` and the affirmative `GATE-B-FINAL-EVIDENCE-MATRIX.md`. Preserve all local changes; no new push/deployment was made. Next work belongs to separately agreed gates, not another rebuild of B0–B8.

Use `GATE-B-COMPLETION-AUDIT.md` for current B0–B8 evidence and `CONTINUE-GATE-B-PROMPT.md` for the bounded requirements. Gate B is active pending final acceptance closure. Final combined regression 59716 passed 986 tests / 198 files against unchanged source; build 19524 passed. No test/build process from that checkpoint remains running. Other running-session references in historical notes are stale. Preserve the dirty worktree and `backup89`; do not deploy, migrate production, activate providers or use Docker.

Current code reaches migration 121 and includes atomic repository/package/website publication, lifecycle generation fencing, mixed-source First Proof guidance and exact-source Overview/Coverage navigation. Real-browser acceptance includes independent reviewer exception approval, redacted proof publication/revocation, viewer restrictions and mobile release preview/detail/Back selection. The final requirement matrix remains under review. Do not rebuild completed features or claim a percentage from test counts.

## Historical checkpoints (not active instructions or process status)

Full run 19160 is TERMINAL: 733/746 passed, 13 failures. Diagnosed fixture-path tests missing explicit NOSPOILERS_INTERNAL_LOCAL_SCAN and sandbox EPERM for CLI pipes/loopback listeners. Fixed only affected describe-block test setup with scoped stubEnv/restore; production guards unchanged. Fixed new HTTP test response typing. Permission-enabled rerun66673 passes all87 tests in six affected files plus typecheck/diff. This is NOT a new full-suite pass. Workspace Alerts work and other B0–B8 acceptance remain open; proceed with bounded pagination/export, aggregate counts, browser and Settings.

ACTIVE full-suite verification: session 19160, report /private/tmp/nospoilers-gate-b-alert-cutover-full.json. Confirmed live with hosted/usage/notification test output. Do not edit source/tests during this run. New workspace-alert-api HTTP regression passes (sign-in, no-store, foreign/viewer/revoked/archive and cross-origin writes, response history without jobs). Typecheck found two TS18046 errors in that new test: `detail` from response.json is unknown at lines42–43; add an explicit response type AFTER full run terminates, then rerun typecheck. No runtime failure inferred from these typing errors. Recent UI adds real source counts, 30-second access refresh and explicit website recheck using existing quota-checked endpoint. See GATE-B-ALERT-CUTOVER.md; full Gate B still open.

Newest UI checkpoint: WorkspaceAlerts mounted for workspace Alerts routes in both WorkspaceBoundary independent shell and WatchRouteContent mixed shell, reusing approved WatchAlertsWorkspace. Feed/detail/response/member picker use workspace endpoints; URL selection/status, export and saved website-attempt links exist. Alerts sidebar available without GitHub. Build/typecheck pass; lint only existing ScanPage warnings. Initial shell regression expected Alerts hidden; updated to require scoped link, rerun 33649. Read newest GATE-B-ALERT-CUTOVER.md for explicit limitations: no browser acceptance, counts/polling/empty semantics and pagination/export scalability outstanding, website recheck missing. Full suite baseline is old; don't claim full Gate B or fully polished Alerts.

Current 101 alert checkpoint supersedes older notes: workspace-owned alert/event migration, atomic website finding publication, workspace list/detail APIs, and acknowledge/resolve/reopen endpoint now exist. Ten focused tests across ownership/website worker/legacy incident response pass; typecheck and diff validation pass. Tests include actual pre-101 history upgrade, duplicate worker/acknowledge protection, viewer/foreign/revoked denial, archive read-only behaviour and unchanged evidence/no response-triggered jobs. Assignment, approved Alerts UI consumption, HTTP acceptance, counts, notification ownership and remaining B0–B8 workflows are still open. Full 740-test baseline below predates these changes. See GATE-B-ALERT-CUTOVER.md; do not claim completion.

VERIFIED FULL CHECKPOINT: suite 81558 completed successfully, **740 tests / 144 files passed**, report /private/tmp/nospoilers-gate-b-current-full.json. Source/test tree stayed unchanged during the run. Build/lint/diff also pass with existing bundle and two ScanPage warnings. No root test session remains active. Next implement the independent website Alerts cutover in GATE-B-ALERT-CUTOVER.md, then remaining Settings/Overview/source/state acceptance. The full B0–B8 ledger remains open; passing tests do not implement missing workflows. Do not mark Gate B complete.

Current verified wait: full suite 81558 remains live and producing output. Source/test tree has not changed during the run; only docs updated. Read new GATE-B-ALERT-CUTOVER.md for the repository-grounded independent-alert migration contract (existing alerts/events require installation; website worker currently publishes result only). Poll 81558 before editing modules. Do not claim this planning document implements Alerts, and do not mark Gate B complete.

ACTIVE unchanged-tree full verification: session 81558, report /private/tmp/nospoilers-gate-b-current-full.json. Poll this exact handle until terminal; do not edit source/tests meanwhile. Build/lint/diff session 97004 completed successfully (existing 504.59kB bundle warning and two ScanPage effect warnings only). Completion-audit table updated to current schema100 evidence and precise remaining scope; no rows closed, no full pass claimed yet. Next independent website Alerts/Settings and connected Overview aggregation remain substantial open requirements.

Latest identity follow-through: countOpenAlerts and WatchBentoBoard now use signed-in member ID, sharing alertAssignedTo with the desk filter; post-response mine-filter reset compares assigned_to_user_id rather than display login. Expanded identity/count regression and 28 desk tests/typecheck/diff pass. React guidance kept counts derived from existing data. Full suite still needs rerun after accumulated changes. Independent website Alerts, workspace Settings, connected Overview evidence aggregation and all other open B0–B8 acceptance remain in scope.

Latest mine-filter continuation: main desk alert filtering and mine count now receive signed-in user.id and compare assigned_to_user_id. When stable actor ID is known, unmigrated name-only records do not match by name; legacy callers without ID retain compatibility. Memo dependency includes actor ID. 28 verdict/desk tests pass with rename, same-name-other-member and unresolved legacy-ID cases; typecheck/diff pass (session 17079). Broader alert-response checks passed 17 previously, including incident-response suite. Remaining login comparisons in view-model summary and post-action filter reset still need audit/cutover, as do independent website Alerts/Settings and other B0–B8 items. No full suite active.

Latest response hardening: acknowledge/resolve/reopen now lock the workspace then alert and recheck responder authority within the write transaction. Concurrent acknowledge is idempotent; concurrent resolve/reopen records only one successful transition/event. New direct-store permission/concurrency test and assignment API regression pass; typecheck/diff pass. Acknowledge handler now returns structured permission errors (small follow-up after checks; rerun next). No full suite active. Stable-ID mine filters, independent Alerts/Settings and all remaining Gate B acceptance remain open.

Latest assignment API acceptance: new HTTP regression proves explicit null unassignment, rejection of anonymous/outsider/viewer and ambiguous/blank requests, unchanged alert evidence/resolution, exactly the assignment/clear audit entries and no scan jobs. Test passes; typecheck/diff session 42596 completed/collect if needed. This supplements the prior five chooser/identity/accessibility tests, not full B5 acceptance. Stable-ID mine filtering, independent website Alerts, remaining Settings/Overview integration and final full suite still open. No deployment or live records changed.

Latest Alerts continuation: assignment API accepts explicit userId:null to clear ownership (blank/ambiguous input still rejected); store transaction rechecks responder membership, clears both internal ID and display login, appends 'Assignment cleared' event, and leaves finding/scan state untouched. Native member picker has deliberate Unassigned option; AlertsScreen serializes that to null, not an empty login. Stable-ID store test passes; final chooser/identity/accessibility/typecheck session 71112 pending collection. No full suite active. Remaining B5 HTTP clear-assignment permissions, stable-ID mine filters, response/exception state acceptance and independent website Alerts still open; do not close Gate B.

Latest first-proof integration fix: FirstProofOrUploads now checks saved evidence by workspace when present, rather than filtering it through the auto-selected GitHub installation. Its history child uses the same workspace scope. This prevents existing website/package evidence being misreported as a new empty workspace. Four transition tests/typecheck/diff pass, including a mixed-workspace website record regression. No full suite running. Connected Overview aggregation and the remaining B0–B8 checklist still require implementation/acceptance.

Newest Overview continuation: authenticated workspace Overview now returns separate websiteCoverage totals (all/attention/delayed), derived from the same health rules as Coverage. UI counts open URL-backed websiteHealth filters; both independent and GitHub-connected Coverage pass the filter through, with clear empty/reset state. First-proof screen remains before saved evidence; website summary appears alongside it when connected. Overview polls when website sources exist so due state updates. Thirteen focused Overview/filter/first-proof/API tests plus typecheck/diff pass before final empty-filter copy (rerun next checks). No full suite active. This does not yet add the summary to the legacy GitHub Overview nor finish independent Alerts/Settings; B0–B8 remain open. Prior six health/refresh tests passed, including cache clearing on access revocation.

Latest Coverage health continuation (schema still 100): list API supplies latest attempt status; shared websiteHealth distinguishes disconnected/paused/unverified/delayed/checking/failed/unchecked/manual/scheduled without claiming global safety. Overdue schedules and last failed checks cannot be masked by older evidence. Coverage refreshes every 30s with abort/timer cleanup, keeps read failures separate from mutation failures, marks stale cached data, and clears cached sources on 401/403/404. Initial six focused health/API/UI tests pass; final expanded refresh/revocation test + typecheck session 24468 (collect result). No full regression run active. Overview aggregate health, independent Alerts/Settings and remaining full Gate B acceptance still open; previous 725/726 full result predates subsequent fixed migration fixture and UI changes.

Latest product continuation: website saved-result briefs now use website decision/scope/remediation language and return to owning Coverage controls rather than requesting artifact uploads. History previews distinguish retrieved website assets; workspace-connected Coverage mounts the independent website controls regardless of selected GitHub installation, while retaining old linked-source controls and suppressing new legacy website creation for workspace URLs. 29 focused detail/history/architecture/mixed-source tests pass; typecheck/diff pass, lint only existing two ScanPage warnings. Final row provenance label changed afterward (rerun with next checks). No active full suite; last full baseline still 725/726 before corrected migration fixture. Remaining: full source health/Overview/Alerts/Settings integration and browser/state acceptance across B0–B8. React guidance kept actions explicit and scope derived, no scan on viewing evidence.

Newest follow-up: full suite 91708 is TERMINAL, 725/726. Sole failing old-origin migration fixture erased modern migration markers but left their constraints; replaced with a genuine pre-verification origin table fixture and verified migration plus no-op rerun. Runtime/UI focused run passes 20; typecheck/lint pass (only existing ScanPage warnings). WorkspaceWebsites clock purity warning fixed with cleaned-up minute timer. ReleasesScreen now scopes saved attempts to workspace, not its automatically selected GitHub ledger, so independent website deep links remain visible in mixed workspaces. New scope UI regression is session 95477 (collect final). No full-suite rerun after these fixes yet, no full process active. Next mixed-source Coverage entry/legacy source preservation, website-specific detail labels/recheck and aggregate health remain necessary; preserve B0–B8 scope.

Current checkpoint 100 (6 September): explicit website schedules (manual/6h/daily) now have protected API, Coverage controls, actor/version/due metadata, and existing poller integration. Admission atomically charges/enqueues/advances next due; no overlapping scheduled scan, live source/member/payer checks, disconnect disables scheduling, changed schedules cancel queued work before crawl/refund. Worker source locks follow workspace locks. Three scheduler tests pass (duplicate/microsecond precision, lifecycle/revocation/refund, real result/viewer/foreign/expired-plan), seven API/UI/worker checks passed before final expanded scheduler test; typecheck/build pass. Full suite is LIVE session 91708, report /private/tmp/nospoilers-schedule-full.json. Do not edit source/tests until terminal; poll this handle. Lint has existing two ScanPage warnings plus a NEW WorkspaceWebsites Date.now-in-render warning: fix after full suite, ideally server-derived due state or stateful clock. Browser Coverage inspection tab9 pending. Mixed-source UI/Overview/Alerts/Settings and all remaining ledger acceptance remain open. User asked percentage: rough 70–75% implementation estimate, not measured/launch-readiness. No Docker or deployment.

Latest restart: loopback local-review server running on 4347 (session 27985); browser confirmed the real saved-evidence Overview. Overview now labels saved website checks separately from artifact scans using server-projected immutable source identity, with neutral aggregate copy and unchanged scoped release links. Typecheck, four focused Overview tests and diff validation pass. Previous full-suite report is absent from /private/tmp; do not claim it passed. Gate B remains open, including website scheduling, mixed-source workspace integration and all remaining audit acceptance. No Docker, deployment or provider activation.

Newest visible website lifecycle checkpoint (099): list API now returns paused state and last 20 source events. Coverage has pause/resume, reconnect, typed exact-URL disconnect with retained-history explanation, and activity details. Paused sources have no scan action. Targeted API/UI/lifecycle tests pass (2 then expanded 3), typecheck/build underway/completed via session 90558. Full suite launched /private/tmp/nospoilers-website-lifecycle-full.json; keep source/tests unchanged until that run finishes. Next substantial work remains automatic monitoring schedule/cadence and workspace Overview/Alerts integration, connected-workspace website UI, and the rest of B0–B8 audit. Do not mark Gate B complete.

Newest lifecycle backend checkpoint: schema 099 adds paused_at and append-only workspace_origin_events. Creation/verification and pause/resume/disconnect/reconnect record events. Protected POST .../origins/:id/lifecycle validates writable workspace, typed URL for disconnect, retained history, fresh challenge on reconnect and connection allowance. Paused sources cannot enter or finish a scan. Lifecycle+website worker tests passed 2; final quota serialisation/ID validation added afterward (rerun needed). UI controls and audit rendering are not yet added; list response still needs paused_at. No automatic scheduler yet. Continue all Gate B, no full completion claim or deployment.

Newest website UI checkpoint (098): protected workspace origin list + explicit check endpoint wired to the shared scan-attempt queue. Client-generated attempt UUID keeps retries idempotent. Independent Coverage now renders WorkspaceWebsites: connect URL, DNS/HTTP challenge, explicit verify, explicit scan, latest attempt link, loading/error/read-only states. Mounted UI does not scan; pending mutation guard and same-origin result navigation included. Browser verified the form in the real app shell at /watch/sources?workspace=71a701c7-7d7a-9705-3426-4710762104d1&configure=website (iab tab7); no real external domain connected. Four focused API/UI tests and typecheck/diff pass. Automatic website monitoring, pause/disconnect/reconnect UI, status/cadence aggregation, audit trail and connected-workspace integration remain OPEN; copy explicitly says scheduling is not enabled. No full suite after 098 yet, no active test sessions. Continue all Gate B requirements, not just website happy path.

Newest website worker checkpoint: migrations 097–098 add immutable source_origin_id to saved attempts. queueUploadedScan accepts an INTERNAL sourceOriginId, validates active verified independent origin and workspace, and uses existing payer/usage/idempotency reservations with kind workspace_origin_scan. Shared worker crawls the saved URL under existing bounds, applies workspace policy, preserves inconclusive status, stores actual digest and signed result, rechecks source/access at publication, cleans temporary bytes and refunds before-processing failures. Uploaded read authority now explicitly excludes revoked members. Real scanner fixture passed website→saved receipt/no GitHub/shared payer; upload ownership tests passed too. Latest retry test/typecheck session 54715 (poll if needed). No public website scan admission endpoint or Coverage UI yet; do not claim customer journey finished. Next wire explicit verified check API, read/status UI, first check after verification, and source/result labels; then broad regression. Current full baseline remains 710/711 before keyboard fix, not a latest full pass.

Newest website API checkpoint (still 096): POST /api/workspaces/:workspaceId/origins creates independent challenged source; POST .../origins/:id/verify performs DNS/HTTP ownership and rechecks live workspace access plus unchanged challenge after outbound verification. Both explicitly return queued:false; scanning is NOT wired yet. Auth/rate limits and malformed-body validation included. API + foundation tests pass 3, typecheck/diff pass. Next implement independent website job admission/payer/policy, worker result persistence and Coverage UI; no GitHub fabrication. No active test sessions remain. Full suite after these changes still required.

Newest website foundation: migration 096 adds immutable workspace ownership to watched_origins, backfills mapped legacy connections, allows installation_id=NULL only with a workspace, and gives independent sources workspace/URL uniqueness without collapsing distinct legacy histories. createWorkspaceOrigin is an INTERNAL primitive with live writable workspace/revocation/plan checks, serialised quota, random ownership challenge, no fabricated installation and no queued work. Legacy origin readers/poller explicitly exclude independent records until their worker path is implemented. Foundation test passes; legacy website suite passed 24; typecheck/diff pass. API, ownership verification, workspace-aware worker/billing/results and independent Coverage UI remain to be wired. Do not claim website-only scans usable yet. No active test handles remain. Preserve all legacy records; next work follows GATE-B-WEBSITE-CUTOVER.md.

Newest website lifecycle checkpoint: migration 095 adds watched_origins.disconnected_at. Disconnect retains the record, revokes deployment tokens and removes it from active reads/polling; reconnect via explicit origin insertion reuses identity and clears verified status while retaining a non-null challenge barrier until fresh verification. Worker rejects absent, cross-installation and challenged-but-unverified origins and crawls only the stored origin URL. Typecheck/diff pass; website+authority suite 26 passed, then expanded authority suite 3 passed including cross-scope no-crawl test. No full suite after 095 yet. Independent website workspace ownership/schema/API/worker cutover still required; no fabricated installation added. The earlier keyboard and upload scope fixes are verified. No active test handles remain.

Newest continuation after full run 86892: terminal 710/711, sole failing assignment keyboard test revealed disabled async-select initial focus. Dialog now focuses its native Close button; test verifies focus containment, tab trapping, Escape and restoration using new chooser. Upload workspace scope now skips organisation bootstrap for already mapped sources and rejects revocation tombstones; binding regression checks no duplicate organisation after upload-scope resolution. Website verification/challenge/token/delete mutations now use effective workspace membership and reject viewers/revoked members instead of trusting stale installation_users. Targeted hosted website/accessibility run passed 27; final typecheck + origin revocation/accessibility checks running session 33590. No new full pass claimed. Next continue website workspace schema/API/worker migration per GATE-B-WEBSITE-CUTOVER.md; origin deletion still needs retained disconnected state. No active full-suite handle remains.

Active verification: full suite session 86892, report /private/tmp/nospoilers-member-identity-full.json. Source/test files have stayed unchanged during this run; only docs updated. Poll this exact handle before proceeding with source edits, or inspect its terminal report if finished. Lint passes with the two existing ScanPage effect warnings. WEB-APP-ARCHITECTURE.md now has an up-to-date 094 summary; GATE-B-WEBSITE-CUTOVER.md records the concrete remaining website-only dependency migration, including stale installation_users mutation checks and worker ownership/verification rechecks. No claim that website-only workflows work yet.

Latest B5 continuation: migration 094 adds alerts.assigned_to_user_id with conservative unambiguous legacy backfill. Manual assignment accepts internal userId (legacy login input still supported); workspace lock and responder/assignee membership are rechecked transactionally. Automatic routing also records internal identity. Alert reads resolve current member name while persisted login and alert event remain historical snapshots. Real assignment chooser loads authorised members through /api/alerts/:id/assignees and submits userId; no automatic assignment on mount. Five focused identity/chooser/permission tests, typecheck and diff pass. No full suite claimed for 094 yet. Remaining B5: full HTTP/rename/routing/mine-count acceptance, unassignment and response-state parity; other B0–B8 rows remain open. Full previous run 5161 terminal 706/707 (one old Store/new test mismatch during edits). Do not edit modules while a full run is active. No live provider changes or deployment.

Newest continuation: schema 092 soft-disconnects repositories instead of deleting identity/FK history; active lists and workers exclude revoked access, routine upserts cannot reconnect or move ownership, explicit repository-added delivery can reconnect in the same installation. Removal is installation-scoped. Schema 093 restores proof-published/proof-revoked audit actions accidentally omitted by 091 (091 also corrected so pre-existing proof events migrate). Initial full run completed 703/706, failures were two old automatic-connection health fixtures and the real proof audit regression; all three fixed, 7 targeted tests/typecheck/diff pass. Earlier retention+hosted run passed 59. Clean full rerun now pending: /private/tmp/nospoilers-repository-retention-full.json. Inspect current live test handle before restarting. Coverage empty workspace entry is implemented/browser inspected. Gate B remains OPEN; do not count pending full verification as passed. Next include retained disconnected sources in Coverage with honest reconnect/health status, audit other source types, and finish B0–B8 ledger. No real history deleted.

Latest local continuation: server confirmed responding at http://127.0.0.1:4347; browser rendered the authenticated artifact Overview with saved evidence. GitHub connection implementation now includes migrations 090–091, signed pending webhook staging, explicit workspace connection start/finish APIs and UI, and transactional fresh binding. Sign-in skips unknown installations rather than attempting an invalid membership insert; completion checks are rate-limited and fail clearly when GitHub credentials are absent. Typecheck and 67 focused tests across hosted, connection API/UI, pending events and workspace binding pass. Full Gate B remains OPEN; use GATE-B-COMPLETION-AUDIT.md. No live GitHub installation, deployment, Docker, provider activation or history deletion was performed. Remaining coverage entry points, inventory reconciliation, source-type parity and full acceptance must still be completed; older full-suite results below are not verification of these newest changes.

Latest restart/continuation (still 089): local-review Vite server is running on loopback `http://127.0.0.1:4347` (exec session 96518). Browser verified homepage → authenticated Overview → existing sourcemap.tgz release detail with three findings. Fixed `upsertInstallation`: update + legacy bootstrap are transactional, and mapped sources do NOT create a standalone organisation/billing trial on webhook refresh. Expanded source-billing regression; **63 focused tests pass** (source billing, hosted webhook/runtime, workspace migration), build/typecheck/diff pass; existing lint/bundle warnings remain. Full 693-test result below predates this small follow-up; no fresh full-suite claim. Fresh GitHub binding coordinator remains unfinished; no new Connect UI, no signed-event staging or callback binding implemented in this follow-up.

Newest continuation: `089_github_connection_intents` adds internal hashed/expiring/session-bound one-use intents with destination authority rechecked and consumption/binding in one transaction. No create endpoint or new Connect UI yet. GitHub port now has paginated installation repository inventory and fixes first-page-only user installations; bounded/validated, no partial success. Final **693 tests / 122 files pass**, build/typecheck/diff pass; existing lint/bundle warnings remain. Full report: `/private/tmp/nospoilers-github-intent-final.json`. All root test/build sessions completed. IMPORTANT: accessible user installations do NOT prove GitHub installation administration. Fresh binder must verify appropriate GitHub authority, stage signed webhooks, coordinate callback order, bind/project access before workers, and resync repositories. Existing setup/webhook defaults are unchanged; no new attachment exposure. Read newest Gate B tracker for exact remaining contract.

Newest continuation: migration `088_job_source_identity` separates the jobs source FK from its immutable payer FKs, preserving legacy orphan aliases with a NOT VALID source constraint while checking new writes. Billing API source inputs now resolve the mapped organisation before administrator checks; explicit mismatches reject, personal billing remains supported. Added shared-account/source-without-billing fixtures and HTTP routing regression. Final clean full rerun: **685 tests / 120 files pass**, build/typecheck/diff pass; existing lint/bundle warnings remain. Report: `/private/tmp/nospoilers-billing-routing-final.json`. All test/build sessions completed. Next fresh-attachment work: session-bound one-use intent, webhook/setup coordination and repository listing/resync; not yet exposed. See newest Gate B tracker. Organisation subscription/scan allowance is shared across workspaces, never multiplied per workspace.

Newest continuation (still schema 087): source-billing.ts resolves source work/coverage/retention/plan reads and hosted enqueue/unpack usage through workspace organisation ownership. Personal and organisation payers flow into immutable job snapshots. Final **684 tests/120 files pass**, build/typecheck/diff pass; existing lint/bundle warnings remain. Completed test report: /private/tmp/nospoilers-source-billing-verification.json. Fresh GitHub attachment remains disabled: billing management/status routing, foreign keys and setup/webhook resync remain. See top Gate B tracker. Estimated local Gate B implementation 75–80%, explicitly not a measured launch-readiness percentage. No Docker, deployment, source reassignment or provider changes.

Newest work: `087_job_billing_owner` applied in code. Jobs snapshot payer separately from source; new independent artifact jobs use installation_id=null with explicit billing keys. Retry/refund/concurrency follows the snapshot, and upload/worker refunds share a once-only reservation. Final **682 tests/119 files pass**; build/typecheck/diff pass, existing lint/bundle warnings remain. Read newest Gate B section and its concrete source-binding contract. Fresh GitHub attachment is still NOT enabled—next work is remaining source entitlement resolution, session-bound intent, setup/webhook coordination and repository-list resync. Disk now has space after the previous write failure; no data was deleted by this task. Final test session completed; no Docker, deployment, provider changes or push.

Newest continuation supersedes older checkpoints: migrations through `086_workspace_scan_policy`; real artifact Overview, retention/audit/policy navigation, redacted proof publication/revocation, workspace-authoritative source reads, targeted repository-alert checks, conservative personal resubscription and scoped deletion-impact review now integrated. Read the newest section of GATE-B-IMPLEMENTATION.md for exact limitations. Final full suite: **679 tests/118 files pass**; expanded workspace API check passed separately. Final build/typecheck/diff pass; existing two lint warnings and bundle warning remain. Live browser inspected Overview, policy, retention impact, audit and private sharing preview. All root test/build sessions completed. Gate B remains open, especially fresh source/billing ownership cutover and deletion execution. No Docker, provider activation, deployment or deletion of user records.

Latest verified checkpoint: migration `083_deletion_withdrawals`; approved retention policy, typed owner deletion-review requests + withdrawals (NOT execution), and authoritative alert-to-release links now integrated. Full suite **648/648 in 100 files** passed; one new HTTP link regression passed afterward. Build/typecheck/diff pass, existing lint/bundle warnings remain. Read newest Gate B section for browser verification caveat and exact remaining work. No deletion/deployment/provider changes. User requested progress percentage: rough planning estimate 65–70% of Gate B implementation, explicitly not measured or launch readiness; do not substitute that estimate for acceptance evidence.

Owner decision now confirmed: disconnect retains history; permanent deletion requires separate explicit typed authorisation, including alongside account closure. Read DATA-LIFECYCLE-POLICY.md. Earlier "retention pending" statements below are historical and superseded. This does not authorise deleting existing records. Deletion-review request workflow is being implemented; execution/personal account closure remain separate work.

Final checkpoint for this continuation: **639 tests in 96 files pass** (full suite with local listener permission), production build/typecheck and diff validation pass. Lint: two existing ScanPage effect-state warnings; build: main chunk just over 500 kB. Browser verified personal organisation billing disabled when Stripe unconfigured, saved three-finding brief, and brief → in-app proof verification preserving workspace. All test/build sessions completed; no hosted services activated. Current migration `080_workspace_connections`. Gate B is NOT complete; the concrete acceptance inventory in GATE-B-IMPLEMENTATION.md is the next work contract. Retention policy still unanswered. Do not report security/enterprise readiness or full source migration as complete.

Current continuation supersedes older checkpoints: migrations through `080_workspace_connections`; personal organisation billing routes/UI and atomic Stripe event claim+application; guarded unused-only connection placement API/Workspaces form; append-only product audit events; honest public proof/error/privacy states. Full source/worker ownership migration, fresh GitHub placement before sync, website-only workspaces, uploaded public sharing/revocation, billing resubscription and remaining B1–B8 acceptance still open. Retention answer pending. Focused tests and local browser personal-billing/saved-brief inspection pass; final suite/build results are being collected. No deployment/Docker/provider changes. Read newest Gate B section before older notes.

Latest continuation adds uploaded-history keyset pagination and server-side status filtering, URL-backed Older/Newest navigation, scoped cursor checks and distinct filtered-empty states. No migration (still 077). Final focused checks: 25 tests/five files passed; typecheck and diff validation passed; lint only two existing ScanPage warnings. Production build passed before final empty-state copy/API regression additions. The earlier full-suite checkpoint below predates pagination. Source attachment/resource cutover remains the largest open workstream. Retention-policy question still pending; no deployment.

Final verification of the 077 continuation: full suite **615 tests / 89 files passed**, final build/typecheck and diff validation passed, lint only two existing ScanPage warnings. Browser re-opened the original saved three-finding release brief successfully. All test/build sessions completed. Local dev session 76820 was confirmed live this turn. Owner's retention-policy question remains unanswered; work can proceed on other Gate B items without deployment. No goal completion claim.

Newest checkpoint supersedes the paragraphs below: current migration is `077_connection_disconnect`, including `076_billing_durability`. Workspace/source routing supports multiple mapped connections without fallback to another workspace. Billing authority is organisation-based; organisation-backed billing records and independent artifact jobs survive GitHub disconnect. Uninstall is now locally implemented as inactive retained history, stopped source work and revoked scan tokens; routine webhooks cannot reactivate it. **Owner confirmation pending** for retained evidence vs old uninstall-deletion policy. Do not deploy; reconcile `src/legal.ts` and `docs/ACCESS-BOUNDARIES.md` after the answer. Source attachment/reassignment, full worker/resource cutover and personal billing remain open. Full rerun is in progress after two legacy deletion-test expectations were replaced with explicit retention/inactivity assertions. Prior full checkpoint: 610 tests; newest focused billing/lifecycle: 16 passed. Read latest Gate B section. No hosted/provider changes.

Newest continuation: migration `075_organization_ownership`, organisation access API/UI and independent artifact-workspace billing-authorisation separation are implemented. The local Workspaces administration panel was browser inspected. Full suite passed 605 tests/87 files. Subsequent effective legacy-role/team display regression passed seven membership tests; final typecheck/build and diff validation passed. See newest Gate B tracker section. Organisation ownership is implemented; durable billing storage/source deletion, connection chooser/reassignment and remaining B1–B8 parity are still open. Do not claim Gate B complete.

Active goal: complete all Gate B, not merely the current slice. Latest code adds draft `074_workspace_membership`, workspace Team/inbox/API flows, commit-time upload access recheck and `WorkspaceEntry` legacy URL resolution. Subsequent full suite passed 603 tests/86 files. A later transactional source-permission projection and GitHub rejoin prevention passed six focused tests; final broad rerun still needed. Read the membership continuation section in `GATE-B-IMPLEMENTATION.md`. Next major work: connection chooser/reassignment, independent organisation ownership and resource/worker/billing cutover, plus remaining B1–B8 acceptance. Auth provider and Docker/deployment remain deferred. Preserve all existing dirty work. Do not claim Gate B complete.

Latest follow-up: storage recovered; `073_workspace_upload_ownership`, mockup-style uploaded release brief, and workspace UI/boundary pass typecheck/build and a 589-test full-suite checkpoint. Browser verified new local workspace → artifact upload → persisted three-finding readiness brief, refresh/scope switching and archive/read/scan-lock/restore. Fixed Create/Save form submission; added a subsequent sidebar mutation-refresh fix with a focused regression test. See `GATE-B-IMPLEMENTATION.md`. Independent workspaces currently cover artifact evidence only; connection/member migration and full UI acceptance remain open. Local dev server restarted on 4347 with opt-in local review. No deletion, Docker install, hosted migration or deployment performed.

Latest Gate B checkpoint: `GATE-B-IMPLEMENTATION.md`. Owner explicitly included workspace migration now, with provider choice later. Current additive migration is `072_workspace_management`; authenticated workspace create/rename/archive/restore APIs and allowance/event records exist, but resource-authorization cutover and switcher/member UI are NOT complete. Full suite passed 580 tests/80 files; build passed; real local browser upload → persisted findings → package-only Overview verified. The local review database contains the repository's sourcemap fixture as a test upload. Follow the remaining acceptance list, not older implementation claims below.

Gate B direction: read `IDENTITY-WORKSPACES.md` for the independent identity/workspace/source model and provisional commercial caps. Preserve installation scope from upload to Releases and from result/retry to New Scan; clear old data on scope changes. Regression tests: `tests/uploaded-releases-ui.test.tsx`, `tests/workspace-management.test.ts`, `tests/workspace-api.test.ts`. Provider login and full resource migration remain open. Master-flow remains primary; Mock 3 remains release-detail reference.

Owner decision, 5 September 2026: do not install or require local Docker. Hosted worker deployment is deferred; Railway is a candidate to assess later. Next work is Gate B product flow and API/state parity. Preserve the security requirement and adapt the existing production container guard when choosing the host; do not bypass it for live uploads. See `GATE-A-IMPLEMENTATION.md`.

Latest checkpoint (5 September 2026): read [Gate A implementation](GATE-A-IMPLEMENTATION.md) before older status notes below. Queued browser/CI artifact processing, installation-ledger completion, viewer enforcement, staging budgets, pinned HTTPS and restricted parser invocation are now implemented. Migration marker is `070_immutable_upload_results`. Container runtime, production-like concurrency/recovery and private deployment verification remain required; no launch-readiness claim or deployment is implied. Preserve existing user changes and mockups.

Read **[`docs/STATUS.md`](STATUS.md)** first. Then [`docs/PRODUCT.md`](PRODUCT.md), [`docs/ROADMAP.md`](ROADMAP.md), and [`docs/ACCESS-BOUNDARIES.md`](ACCESS-BOUNDARIES.md). Do not treat this file as a second feature list.

## Repository

Current build handoff: read `ENTERPRISE-READINESS-PLAN.md`, `WEB-APP-ARCHITECTURE.md`, `SECURITY-READINESS-REVIEW.md`, and `MOCKUP-PRODUCTION-CONTRACT.md` before older status claims. Browser scans now queue durable upload jobs and persist reports/proof in Releases. New migration marker: `066_uploaded_scans` (next unused: `067_*`). Local review login is opt-in with `NOSPOILERS_LOCAL_REVIEW=1`; never enable it on a public host. The entire dirty worktree contains ongoing user work; do not discard or treat every change as this implementation's work.

- Private GitHub: `EmotiveImpact/nospoilers`
- Default branch: `main`
- Local app: `npm run dev` → **http://127.0.0.1:4347** (not 3000)
- Watch is the 2B monolith against live APIs. Ember is homepage-only. Do not invent customer rows.
- Authentication: `/watch` is session-only; legacy `/watch?as=trial|ended` parameters are ignored. Anonymous Scan remains available, including `/scan?as=ended` for its locked-state study.

## Database

- Neon project `NoSpoilers`, branch `production`, database `neondb`. Neon Auth stays off.
- App boots from `DATABASE_URL` (Runtime Secret + gitignored `.env`). Without it, local PGlite under `data/`.
- `/api/health` reports `{ database: { mode: "neon" | "postgres" | "pglite" } }` and never the URL.
- Next unused migration id is **`063_*`**. Applied through `062_release_signing_policies`.
- Receipts are HMAC `dev-hmac` in development. Production signing should move to KMS.
- Tokens and webhook/Jira/PagerDuty/Slack/SIEM/email secrets are AES-GCM (`ns1.`). Never returned after save.
- Scan API tokens are SHA-256 hashes (`nsp_` shown once). Deploy tokens are `nsd_` hashes.

## Live GitHub facts

- App: `nospoilers-dev`. Install **`158159401`** on `EmotiveImpact` (all repos).
- Contents write and Pull requests write are accepted. Members read is requested, not accepted. Checks write is not requested. **Administration is not granted. Do not grant it. Do not request Workflows write.**
- Throwaway: `EmotiveImpact/nospoilers-throwaway` (repo `1353409756`). Proven: created-public, cheap `.env`/`.map` push, private → public (jobs 52/53 → alerts 41/42), fixture `release_scan` (`phase1-fixture`, failed-policy MAP-001/002/003).
- Setup PR (leave open, never merge): https://github.com/EmotiveImpact/nospoilers-throwaway/pull/1
- Scripts: `npm run phase1:throwaway`, `phase1:visibility`, `phase1:setup-pr`. Do not invent `-vis`. Do not publicize a product repository. Do not transfer.
- Optional `GITHUB_PROOF_TOKEN` only on that disposable repo. The product-repo user token 403s writing the throwaway.

## This host

| Flag | Value |
| --- | --- |
| Stripe | Wired. No keys. `stripe: false`. Checkout 503 |
| Resend | Wired. No keys. `resend: false`. Send 503. Do not mail disclosures or invites |
| Process | `NOSPOILERS_ROLE=web\|worker\|all`. Enqueue `NOTIFY`s `nospoilers_jobs`. LISTEN reconnects. Recovery 15 minutes |
| Railway | Defined, not deployed |
| Domain | `nospoilers.dev` not cut over |

`ADMIN_TOKEN` opens `/internal/prospects`. Create a new long random token; do not reuse a leaked one. `GITHUB_DISCOVERY_TOKEN` is optional.

## Do not

- Describe Stripe, Resend, or Railway as live.
- Grant App Administration or Workflows write.
- Merge the throwaway setup PR.
- Invent tenant data, `-vis` repos, or queue counts.
- Start Electron / SBOM / Sigstore verify / scheduled CDN.
- Build Employee Public Footprint in this repository.
- Buy Stripe, Resend, Railway, or DNS without approval.

## Next prompt

```text
Read docs/STATUS.md, docs/PRODUCT.md, docs/ROADMAP.md, docs/HANDOFF.md,
and docs/ACCESS-BOUNDARIES.md.

Specified product code is in. Launch is Stripe keys, Resend keys, Railway,
and nospoilers.dev — all human-gated. Watch chrome follows shot C
(/mockup-review/2b/21-stage-linear.html?shot=c): treatment on .watch-stage
only, rail stays #09090b.

Do not grant Administration. Do not merge throwaway PR #1. Do not invent
-vis. Do not mail disclosures. Next migration is 063_*.
```
