# Gate B implementation tracker

## Current checkpoint — 8 September 2026

**Gate B accepted:** final regression38375 passes986 tests/198 files; build27665 passes. The requirement matrix and completion audit record the closure. This supersedes the pending-status paragraph below and all historical next-work notes. No source/test/build process remains active.

The entries below are historical implementation records, not a queue of features still to build. Current code includes migration `121_repo_connection_generation`, real workspace-aware GitHub connection APIs/UI, independent membership and billing ownership, source lifecycle controls, and atomic repository/package/website evidence publication. Do not restart the old schema 089 prerequisite work described below.

Use `GATE-B-COMPLETION-AUDIT.md` for current acceptance evidence and `CONTINUE-GATE-B-PROMPT.md` for the unchanged B0–B8 scope. Final combined regression 59716 passed 986 tests in 198 files against unchanged source; build 19524 passed. No test/build from this checkpoint remains running. Gate B is not yet declared complete; the final evidence matrix is being checked.

## Follow-up — local server and mapped-source refresh protection

- Still schema 089. `upsertInstallation` now atomically updates source metadata and only bootstraps legacy organisation/billing when no workspace mapping exists. This closes a leftover webhook path that could manufacture a second trial/account for an additional mapped source.
- Regression now refreshes an additional mapped source without its own billing record and asserts no standalone organisation/account is created. **63 focused tests pass**, production build/typecheck/diff pass; existing lint/bundle warnings remain. The previous full 693-test checkpoint is not a new full-suite result for this follow-up.
- Local server restarted on loopback 4347 with opt-in local review; homepage, authenticated Overview and saved three-finding release detail verified in browser. No live GitHub configuration or customer source movement.
- Fresh binding coordinator, signed-webhook staging and callback-order handling remain next; this follow-up fixes existing source refresh behaviour, not the entire handoff.

## Current continuation — GitHub connection intent and inventory prerequisites (089)

- Migration `089_github_connection_intents` adds ten-minute, hashed, single-use connection requests bound to the initiating session, user, organisation and workspace. Starting again revokes that session's pending request. Session deletion removes pending state; expired sessions/requests fail closed.
- Creation and consumption require current organisation and effective workspace administrator authority on an active destination. Consumption locks and rechecks those boundaries, and the future binder executes in the same database transaction: binding failure rolls consumption back. Tests cover replay/concurrent consumption, cross-session/user use, replacement, expiry, archive, lost roles and rollback.
- **Internal prerequisite only:** there is no public create-intent endpoint or new Connect control yet; setup/webhook binding is not enabled. Do not describe the intent primitive as a completed GitHub connection feature.
- GitHub port now supports installation-authenticated repository inventory. Both that inventory and user-installation listing paginate at 100 per page on a fixed GitHub origin, reject redirects, and reject duplicate/inconsistent/incomplete/oversized results rather than returning a partial inventory. Collection requests have a 30-second deadline and 10,000-record ceiling; a larger organisation needs explicit support, not silent truncation. Repository inventory is not yet wired to resync. Optional port method permits older test adapters; future coordinator must require it rather than treating absence as an empty list.
- Authority finding from [GitHub's installation API documentation](https://docs.github.com/en/rest/apps/installations): `listUserInstallations` includes installations for which a user merely has repository access. It is **not proof of GitHub installation administration**. The fresh binding coordinator must verify installation-management authority (or an appropriate verified installer action), not simply reuse the existing legacy setup membership check. Existing legacy setup is unchanged in this checkpoint.
- Next: signed installation webhook staging, callback-first/webhook-first coordination, verified installation authority, atomic fresh binding + membership projection, authoritative repository resync, and only then expose the workspace-aware connection UI. Preserve existing bound/populated connections; do not create an extra trial while awaiting binding. Repository inventory reads are not an atomic GitHub snapshot; reconciliation must account for repository events during pagination.
- Final verification: **693 tests / 122 files pass**. Production build/typecheck and diff validation pass; the existing two ScanPage lint warnings and bundle-size warning remain. Report: `/private/tmp/nospoilers-github-intent-final.json`. No live GitHub requests, deployment, Docker, provider changes or customer data movement; no new browser acceptance claimed for these backend prerequisites.

## Current continuation — organisation billing routing and job source identity (088)

- Billing status, checkout and portal requests made with a source ID resolve its current workspace organisation before checking organisation administrator authority. Explicit mismatched organisation/source pairs fail closed. Canonical account reads remain independent of source resolution; personal organisations retain their personal billing path.
- Migration `088_job_source_identity` removes the job-source foreign key to billing accounts. New source references point to installations; immutable payer foreign keys remain unchanged. The source constraint is NOT VALID deliberately: historical aliases already lacking an installation are retained, while new/changed source references are checked. No historical jobs or customer data are deleted or rewritten.
- Regression fixtures cover a source without its own billing account, shared payer snapshots, invalid source rejection, mismatched organisation rejection, outsider denial, and HTTP billing status/portal routing to the owning organisation.
- Final verification: **685 tests / 120 files pass** on a clean full rerun after correcting a redundant legacy source-access check on billing status. Production build/typecheck and diff validation pass; two existing ScanPage lint warnings and the existing bundle-size warning remain. Report: `/private/tmp/nospoilers-billing-routing-final.json`. No provider activation, deployment, Docker or live customer source movement; no new browser acceptance claim for this backend-only checkpoint.
- Next: one-use expiring session-bound connection intent, webhook/callback coordination, and authoritative GitHub repository listing/resync. Fresh attachment remains disabled pending these pieces. This is not completion of Gate B or Stripe current-state reconciliation.
- Terminology: the organisation shares its subscription, trial and scan allowance across its workspaces. Creating a workspace does not multiply that allowance. Scan policy defines pass/fail criteria, not subscription usage limits.

## Current continuation — source entitlement and usage resolution

- Source work eligibility, coverage/retention reads and installation-list plan metadata now resolve through the mapped workspace's organisation. Unmapped legacy sources keep their existing account; a mapped source cannot fall back to a standalone trial when its organisation is expired or has no account.
- Hosted source enqueue/unpack usage resolves to that organisation or personal payer. Jobs store the resolved payer in the immutable 087 fields; retry/refund still follows the snapshot, not later mappings. Personal-source heavy jobs share payer concurrency, not just uploaded jobs.
- Retention writes update the resolved organisation billing account; unsupported personal-source retention writes fail instead of creating a new trial account.
- No schema migration beyond 087. Two new isolated source-billing regressions exercise organisation/personal mapping, quota sharing, expired entitlement, concurrency and retained job ownership. These tests use explicit database fixtures, **not an enabled cross-organisation reassignment feature**.
- Remaining before exposing fresh attachment: source-aware billing management/Stripe status routing, source foreign-key assumptions for new connections without standalone accounts, one-use connection intent, webhook/callback coordination and authoritative repository resync.
- Final verification: **684 tests / 120 files pass**. Production build/typecheck and diff validation pass; existing ScanPage lint and main-bundle warnings remain. Full report: `/private/tmp/nospoilers-source-billing-verification.json`. Backend-only changes; no new browser/hosted-provider acceptance claim.
- User-facing progress estimate: approximately **75–80% of local Gate B implementation**, not measured acceptance coverage, enterprise readiness or production launch percentage.

## Latest continuation — durable job billing owner (087)

- Background jobs now store immutable billing-account or personal-payer keys separately from the installation they operate on. Existing jobs are backfilled without charging usage again; already-refunded uploads reconcile their reservation flag.
- New independent artifact jobs no longer pretend their organisation's billing installation is their scan source. Queuing still uses the existing workspace billing owner; no new trial or account is created.
- Retry charging and refunds use the saved payer and reservation day. Upload failure cleanup and worker refunds consume the same reservation, preventing a double refund. Personal retries now reserve usage again and reject expired entitlement.
- Heavy concurrency is grouped by the paying account rather than the submitting member. A personal-job OR-condition bypass found by the new regression is fixed.
- This completes a **job-billing prerequisite**, not fresh GitHub attachment. GitHub setup/webhook coordination, canonical source billing resolution in remaining source operations and safe attachment into a chosen workspace remain next. No customer source was reassigned; no provider, deployment or Docker change.
- Final verification: **682 tests / 119 files pass**. Production build, typecheck and diff validation pass; existing two ScanPage lint warnings and bundle-size warning remain. Focused tests cover migration repeatability, disconnected-source independence, immutable ownership, shared-payer concurrency, retry entitlement and double-refund prevention. This was backend-only; no new browser visual acceptance is claimed.

### Next source-binding implementation contract

1. Resolve source entitlements/usage through the mapped workspace organisation everywhere; current `installationWorkBlock`, source billing reads, hosted enqueue/reserve/refund and billing management still assume a source account alias.
2. Add a one-use, expiring, session-bound connection intent with destination workspace/organisation authority rechecked at callback. Source access and product organisation membership must remain distinct.
3. Coordinate installation and repository webhooks with callback binding. Today `installation.created` persists repositories immediately, and generic webhook `ensureInstallation` can create default billing ownership. Do not discard repository events or create a second trial while waiting for selection.
4. Add an authenticated GitHub repository-listing port for authoritative post-binding resync; current port only lists user installations and reads an individual repository. Verify pagination and permission scope before implementation.
5. Commit binding and membership projection before enabling worker processing; reject replay, revoked/archived destination, foreign installation and already-bound ownership. Preserve legacy defaults and existing evidence instead of silently moving populated connections.
6. Only then expose the workspace-aware Connect GitHub control and run callback-first/webhook-first, tenant-isolation, quota-sharing and complete source-list browser acceptance tests.

## Current continuation — workspace-backed product flows (5 September)

This section supersedes older open-item statements below. Gate B is **not complete**.

- Source access: explicit workspace membership now governs installation roles and evidence list/get reads, including revoked/stale projections. Archived access is read-only. Upload worker rechecks effective membership before publication. Fresh GitHub attachment still requires replacing the source-to-billing-account identity assumption and coordinating webhook-first repository sync; populated-source migration and website-only workspaces remain open.
- Independent artifact Overview: full-workspace aggregate counts, actual recent attempts, active-job refresh and scoped next actions. Empty workspaces retain the approved four-circle guide. Historical flagged attempts are not labelled current incidents; unknown report statuses never count as passing.
- Independent Retention and Audit screens are routed and navigable. They describe enforced retention and workspace-management events, not fictional retention expiry or comprehensive source/billing audit. Artifact sidebar/palette hide unavailable connection-only actions.
- Migration `084_upload_proof_sharing`: explicit admin publication of a whitelisted summary, opaque hashed public tokens, replacement/revocation, immutable snapshots and audit events. Full signed evidence remains private. Public JSON is a recorded summary, **not independent signature/issuer verification**. Rate-limited public reads do not start scans. Revoked and unknown links have identical unavailable responses.
- Alert rechecks: actual repository-backed release alerts offer the existing protected latest-release scan operation. New results never silently resolve historical alerts. Unsupported alert kinds link to Coverage rather than inventing an operation.
- Migration `085_personal_stripe_event_order`: conservative personal resubscription supports exact organisation/customer matching, terminal prior subscription, newer signed events and retired-ID protection. Ambiguous/out-of-order cases fail closed; provider-state reconciliation remains required. No live billing configuration changed.
- Migration `086_workspace_scan_policy`: independent artifact strict policy is persisted with revision conflicts and append-only history, exposed via authenticated API/UI and captured by the worker before parsing. Policy hashes preserve content provenance. Connected workspaces keep their existing source policy; this does not add independent waivers or exceptions.
- Deletion review now has an owner-authorised, scoped impact inventory. It counts current evidence/jobs/connections/public links and states that holds and backup constraints are not checked. It is **not deletion execution** or personal account closure.
- Final verification: **679 tests / 118 files passed**; the expanded workspace HTTP regression (policy origin/role/revision and deletion-impact access) also passed separately. Final production build/typecheck and diff validation passed. Lint has no errors, with two pre-existing ScanPage effect warnings; main bundle still exceeds 500 kB. Live browser confirmed Overview counts, Retention controls and real impact inventory without the earlier error, honest empty Audit, read-only inspection of policy controls, and private proof-sharing preview on the original saved release. No customer history was deleted or publicly shared. React guidance informed keyed scope resets, abort-safe state and capability-aware navigation.

Still open: fresh GitHub binding and populated-source/billing alias cutover; website-only monitoring; independent notification/token settings; unsupported-source rechecks; provider billing reconciliation; recent-auth/holds/backup-aware deletion execution and personal closure; full responsive/live-provider acceptance. Docker, deployment and auth-provider selection remain deferred.

Policy update: owner approved retaining evidence after disconnect and separately typed deletion authorisation, including for closure. DATA-LIFECYCLE-POLICY.md supersedes all older pending-policy notes below. Approval does not mean a purge/account-closure implementation exists or authorise deleting current records.

## Latest continuation — approved lifecycle and linked release evidence

- Migrations through `083_deletion_withdrawals`: owner-only, scope-bound typed deletion requests with explicit acknowledgement, immutable authorisation and withdrawal records, repeat-safe pending requests and new authorisation after withdrawal. Workspaces exposes history-review controls and organisation-closure review separately; requests persist across reload via GET. No purge, personal account closure, subscription cancellation or support notification is implemented by this form.
- Public retention wording now reflects the approved disconnect-retains-history policy. DATA-LIFECYCLE-POLICY.md records remaining execution/hold/recent-auth/backup obligations. Older pending-policy notes are superseded.
- B5: explicit immutable alert-to-release-revision associations, supplied by GitHub/npm/website scan producers, support multiple artifacts. Alert creation and associations are atomic. Scoped read API and related-release UI preserve source/workspace; legacy alerts never infer a link. No targeted rescan action was added.
- Verification: **648 tests / 100 files passed** in the full run; an additional HTTP association regression added afterward passed separately. Build/typecheck and diff validation pass. Lint retains two ScanPage warnings; main bundle remains over 500 kB. React review guided scope resets, disabled confirmation and honest async states.
- Browser inspected the typed-confirmation controls and disabled submit without submitting a real deletion request. Saved-request list showed a transient/unresolved error during live module/migration changes; its final browser retry was not confirmed because the browser context changed. API and UI request/withdrawal tests pass, but repeat the live browser reload check before acceptance. No customer history deleted.
- Gate B remains open. Next critical path: fresh GitHub binding/full populated-source and worker migration plus website-only workspaces, then independent settings/daily Overview, proof publishing/revocation, remaining billing lifecycle and full browser acceptance.

5 September 2026. In progress, not complete. Reference: master-flow prototype; Mock 3 supplies release-detail inspiration. Preserve the production backend and existing user changes. Owner explicitly requested independent workspace migration now, provider selection later; no local Docker or deployment work.

## Verified implementation slices

- B2/B4: workspace-preserving upload → result → new-attempt navigation; scope changes clear old upload UI; scoped previews exclude other installation results.
- B2: real transmitted-byte upload progress, timeout/network feedback, stop-transfer action, duplicate-click suppression and client size/empty checks. Stopping upload does not claim to cancel an accepted server job.
- B4/B6: queued/running/failed/inconclusive/missing-report/failed-policy/passed-with-exceptions verdicts; unavailable deep links differ from empty workspaces; transient detail errors offer retry. Finding details, manifest, engine/policy/time provenance and signed-record explanation are available on uploaded results. Re-upload creates a new record rather than rewriting old evidence.
- B5: production status navigation now Open / In progress / Resolved; assignment is a separate URL-backed filter. Open excludes acknowledged in-progress alerts. Legacy `tab=mine` links remain supported.
- B0: additive `071_workspace_foundation` creates organisations, workspaces, membership, installation connection and linked-identity tables. Backfill preserves separate legacy installations and personal ownership, without tenant merges. New sessions bootstrap corresponding workspaces. `/api/workspaces` is authenticated and filters revoked legacy access during transition.
- B0: `072_workspace_management` adds organisation-level workspace allowances and creation/rename/archive/restore events. Authenticated POST/PATCH APIs lock the organisation before checking the allowance; archived workspaces consume no active slot and restoration checks the limit again. New workspaces copy no GitHub connections. Legacy defaults and connected workspaces cannot be archived before worker/resource cutover. No new trial, billing account or scan usage bucket is created. Initial allowances (2, or 5 for existing Team organisations) are provisional, configurable per organisation and not a published pricing commitment; confirm before rollout.
- B1/B7: package-only customers now leave First Proof after actual uploaded evidence exists. Failed history requests show retry, not a false empty state. Setup is labelled Connection diagnostics, with optional connection checks rather than mandatory onboarding.
- B5/B8: viewer invitation/promotion/demotion controls match the existing read-only server role. Alert tabs support keyboard navigation; unavailable deep links never substitute another alert, and successful status changes follow the same alert into its new queue.
- B2: New Scan checks permissions before enabling uploads, distinguishes viewer/suspended/unavailable/expired states, and resets/aborts upload UI on installation changes.

## Verification checkpoint — 5 September, local only

- Full suite: **580 tests in 80 files passed**, `NOSPOILERS_INTERNAL_LOCAL_SCAN=1 npm test -- --maxWorkers=2`.
- Production build and typecheck passed. Lint has no errors; two existing ScanPage effect-state warnings remain.
- Browser, opt-in local-review account: status + independent assignment produced `/watch/alerts?tab=waiting&mine=1`; New Scan stayed in the product shell. Uploaded repository fixture `fixtures/sourcemap.tgz` through the file chooser, observed processing then persisted `Review findings`, three findings, digest, timestamps and signed-record action. Overview changed to `Your release evidence` without connecting GitHub.
- Local review database now contains that test upload (`70fb03ba-071a-4a78-92a2-f059e3abe0ba`), not customer data. No hosted database migration, push, auth provider configuration or deployment performed.
- Browser review also identified and fixed a misleading empty repository-ledger panel beneath personal evidence and an explicit-install upload-preview scoping issue. Final build/focused checks cover this follow-up.

## Remaining acceptance work

### Organisation billing, guarded connection placement and proof states — current continuation

- Migrations `078_product_event_integrity`–`080_workspace_connections` add database append-only protection for product workspace/organisation/billing events, personal organisation Stripe bindings without synthetic GitHub installations, and explicitly placed connection provenance/events.
- Personal organisation billing now uses the existing Workspaces billing panel and authenticated checkout/portal routes. Signed billing events update the existing shared user entitlement; workspace creation cannot restart the trial or multiply usage. Event claim and entitlement application are atomic, with a failure/retry regression. No Stripe provider was activated. Safe replacement of a cancelled subscription with a new subscription remains open.
- Administrators can place an **unused** GitHub connection within the same organisation from Workspaces. The server requires organisation, both workspace and source authority; any resource/configuration/credential/history blocks movement. Destination membership replaces source access, original workspace membership is retained, and later GitHub login cannot add access to an explicitly placed connection. This is NOT historic resource migration or fresh OAuth-install binding before repository sync; those remain major B0/B2/B3 gaps.
- Public proof screens distinguish unavailable links, retryable errors, malformed responses and digest mismatches. Unknown and withdrawn links deliberately share an anti-enumeration response. Recorded policy decisions do not claim issuer/signature validation. Uploaded proof remains private/download-only with a privacy warning and an in-app verification action. Redacted uploaded-proof publishing/revocation and issuer trust contracts remain open.
- Browser inspected the saved three-finding uploaded brief and personal organisation billing panel, which correctly disables payment on this unconfigured host. Focused proof, billing, connection and integrity tests pass; full suite/build verification follows below. No Docker, deployment, live customer migration or provider activation.
- Retention decision remains pending. All remaining B1–B8 acceptance below still applies; this continuation does not close Gate B.
- B5 follow-up: response permissions now derive from the selected installation role. Viewer/unknown access cannot acknowledge, resolve, reopen, assign or edit response notes; permission loss closes assignment. Server enforcement remains unchanged. Six permission/accessibility tests passed, including existing focus-trap/Escape behavior with an explicitly permitted responder.

### Concrete acceptance inventory after this continuation

Final verification: **639/639 tests in 96 files passed**. Production build/typecheck and diff validation pass; two existing ScanPage lint warnings and the >500 kB main-bundle warning remain. The migration fixture was corrected to remove all later markers when simulating an old database; the existing alert keyboard fixture now explicitly grants responder access. Browser confirmed personal billing's unconfigured state and saved brief → workspace-preserving in-app proof verification. This closes verification for the implemented slice, not B0–B8 as a whole.

- **B0/B2/B3:** full source ownership remains the critical path. Unused-only placement is deliberately limited; fresh GitHub callback binding before sync, existing resource/worker scope migration, and website-only workspaces are not implemented by it.
- **B1:** the honest empty four-circle guide and empty-to-evidence transition exist. Dynamic evidence-driven intermediate stage progression remains absent.
- **B5:** source-backed alert response APIs exist and viewer controls now match them. Related-release navigation, targeted recheck and next-action guidance remain missing. Assignment is still installation-login based, not the final internal-member model.
- **B7:** `FirstProofOrUploads` shows saved artifact history, not the full daily decision/counters/next-action dashboard. Connected `WatchOverview` has real alert/source/job data but still needs release aggregation and acceptance.
- **B8:** independent `ArtifactWorkspace` routes for policy, retention, notifications, tokens and audit fall into a connection-settings placeholder. Connected settings already use real APIs and should be tested/reused rather than rebuilt.
- **B6/billing:** uploaded public sharing/redaction/revocation, issuer trust contract, and safe replacement subscription lifecycle remain unfinished. Live OAuth/Stripe/worker acceptance is separately deferred; absent credentials are not a reason to call these implementation gaps complete.

### Release-history completion follow-up

- Added bounded keyset pagination for uploaded history with deterministic timestamp/ID ordering. Status filters run on the full scoped history before pagination, not only the newest 50 rows. The selected cursor is authorised against the requested workspace/installation. Invalid/stale/foreign cursors fail explicitly.
- Older/Newest navigation is URL-backed (`uploadBefore`), retains workspace/source/status, and clears the previous selection. Scope/page/filter changes remount the history state so previous rows are not displayed under a new context. Empty filters differ from first-use empty workspaces; stale page links can return to Newest.
- Final focused verification: 25 tests across five files passed, including empty-filter and API-validation regressions; typecheck and diff validation passed. Lint has only the two existing ScanPage warnings. Production build passed before the final empty-state copy/API regression additions. The earlier 615-test full-suite checkpoint predates this pagination change. No migration or customer-data change was needed.
- Remaining work is still substantial: source attachment and resource/worker cutover; personal/fully shared billing; proof sharing/verification and remaining release parity; daily Overview/Alerts/Settings integration; full browser acceptance. This is not just visual polish. The completed history work removes the newest-50-only limitation, not the remaining Gate B requirements.

### Connection scope and billing durability — latest continuation

- Workspace responses now return one row per workspace with all connection IDs. The source selector is restricted to that workspace; secondary-source links remain selected, and foreign/unavailable selections fail closed. Interactive/CI uploads resolve the actual mapped workspace rather than assuming the legacy default. Source write permission is checked before reserving an upload. Ten scope/migration tests and eighteen ownership/pipeline tests passed; the pre-billing full-suite checkpoint passed 610 tests.
- `076_billing_durability` binds installation-backed billing accounts immutably to organisations, preserving existing IDs as compatibility aliases. Usage and job billing references no longer cascade from a GitHub installation. Personal subscriptions and general multi-connection billing/resource cutover are still unfinished.
- `077_connection_disconnect` upgrades the local draft and records disconnected sources. Uninstall stops pending/running source jobs, fails pending source uploads and revokes scan tokens, while retaining history and independent artifact jobs. Routine metadata updates cannot reactivate the source; explicit installation creation/verified reconnect is required. Reconnect does not revive old tokens or restart the billing trial.
- Billing checkout/portal now require organisation administration, not GitHub/workspace admin rights. Organisation-scoped billing status/checkout/portal and a real administration-panel billing component exist. Payment controls are disabled when Stripe is unconfigured. Billing events are stored independently; append-only enforcement for new product event tables remains to be hardened. No Stripe requests were made outside test doubles.
- **Policy confirmation pending:** asked the owner whether GitHub disconnect retains evidence with deletion handled separately. The implementation follows that proposed model locally, but old `src/legal.ts` and `docs/ACCESS-BOUNDARIES.md` still promise tenant deletion on uninstall. Do not deploy until the owner confirms the policy and those documents are reconciled. This is not a change to live customer retention.
- Browser rechecked local Overview and Workspaces after migration. Existing personal/QA evidence and workspace controls still load. No live GitHub connection or Stripe account is configured here; their lifecycle coverage is integration tests, not a live-provider claim.
- Latest verification: **615 tests in 89 files passed** after changing two obsolete uninstall-deletion tests to assert stopped work and retained history. Final production build/typecheck and diff validation passed; lint has only the two existing ScanPage warnings. Browser reopened the original saved release brief after the migration and verified its three findings/provenance. Gate B remains open: new connection attachment/reassignment, workspace-first source/worker ownership, personal billing, B1–B8 parity and broad browser acceptance.

### Organisation ownership continuation — 5 September

- Added `075_organization_ownership`: explicit organisation owners/administrators, last-owner protection and administration events. Workspace administrator invitations do not grant organisation administration. Source-role demotion does not remove independently held organisation ownership. Creating workspaces requires organisation administration; renaming/archiving uses workspace authority.
- Workspaces exposes real organisation administration controls. Browser inspected the local owner/allowance/activity panel without changing access.
- Independent artifact-workspace members use the existing organisation allowance without gaining access to its legacy GitHub source. Suspending that unrelated source does not prevent independent artifact scanning. A regression covers invitation, upload, worker completion, shared usage and absent source permission.
- Billing storage still uses legacy installation/user keys. Source deletion and full worker/resource/connection cutover remain open; this is not completed billing migration or Gate B.
- Full regression checkpoint: **605 tests in 87 files passed**. A subsequent effective-legacy-team-role display correction passed all seven membership tests; final typecheck/build and diff validation passed. Lint has two existing ScanPage warnings. No Docker, hosted changes or deployment.

### Membership and legacy-entry implementation — current continuation

- Added draft migration `074_workspace_membership`: explicit product membership provenance, revocation tombstones, account-bound seven-day invitations and access event metadata. Create/accept/revoke/list invitation and team/member APIs are real, authenticated and origin-protected. Invitations require active trial/Team coverage and are rate-limited. Acceptance grants only the named workspace, uses an internal account ID, and does not grant GitHub credentials or a new trial.
- Added production Team & access and an invitation inbox under Workspaces. New invitations currently target an existing NoSpoilers account name; email delivery/provider onboarding stays deferred until provider selection. Viewer controls, role saves, removal confirmation, pending revocation and access activity are implemented. The real local Team & access screen was browser inspected; no customer invitations or access changes were made.
- Added last-owner/concurrent-removal tests, recipient/expiry/revoked-inviter checks, cross-origin API checks, sibling-workspace isolation and subscription-owner quota tests. A worker now rechecks workspace/archive/access before committing parsed evidence; revocation during parsing yields no saved report/proof.
- Legacy unscoped product URLs now resolve to one explicit workspace before displaying evidence. Legacy upload deep links use immutable upload ownership; conflicting installation links fail closed. Browser verified that `/watch/releases` becomes a scoped URL and no longer mixes two workspace histories under one sidebar label. `WorkspaceEntry` keeps the page file as an orchestration boundary.
- Verification: the architecture-boundary failure was fixed by extracting `WorkspaceEntry`; the subsequent full suite passed **603 tests in 86 files**. Build/typecheck pass; lint has only the two existing ScanPage warnings. A later connection-permission compatibility projection is covered by six focused membership tests, including source API access, legacy role/remove API consistency and inability to resurrect removed membership through either GitHub linking path. A final full run after that follow-up is still needed.
- Explicit accepted memberships now project into the existing installation permission table transactionally. Role changes/removal affect connected source APIs immediately; the old role/remove APIs delegate explicit memberships back to workspace checks. GitHub linking honors workspace revocation tombstones and old GitHub invitations cannot overwrite explicit product roles. This is a compatibility step toward workspace-first source ownership, not completion of connection reassignment or billing cutover.
- **Still required:** connection authorization/resource/worker/billing cutover and connection chooser, independent billing-organisation ownership, remaining B1–B8 parity and broad browser acceptance. Explicit members without matching legacy installation access must not be routed through another installation's controller. Do not mark Gate B complete from this membership slice.

### 5 September follow-up: storage recovered, workspace browser acceptance progressing

- Uploaded Releases now uses the approved master-detail history and an explicit full readiness brief (`uploadView=detail`). The brief has a real decision, provenance, category-filtered findings/evidence, manifest and exceptions. It does not copy the prototype's fabricated four-check score. Browser verified the local saved source-map result in this layout.
- `073_workspace_upload_ownership` backfills upload workspace/billing ownership and protects it from changes. New workspace uploads share the existing organisation usage account, preserve separate histories, and recheck archive/access state before worker execution. Focused ownership/pipeline tests passed (21 tests in 3 files).
- Workspace switch/create/rename/archive UI and URL boundary have been added. Installation-connected workspaces route into the existing scoped product; independent artifact workspaces do not load unrelated installation datasets. **New independent workspaces currently support artifact evidence, not migrated GitHub/website connections or full member/settings workflows.** Do not represent this as completed Gate B.
- Storage recovered to about 8.5 GB free. Typecheck, production build and the **589-test full suite (83 files)** passed after migration 073 and the form fixes. New release-brief tests cover keyboard filters, stale finding links and unfinished evidence. Browser testing caught non-submitting workspace Create/Save buttons; explicit submit types and regression tests now cover them. A subsequent sidebar invalidation fix refreshes workspace labels after mutations and has its own focused regression test.
- Browser verified the real local workflow: create `Gate B local QA` (`da57be82-c929-4f09-8b5e-25d7a13a5e2d`) → New Scan in the product shell → upload `fixtures/sourcemap.tgz` → worker completion → saved three-finding result (`848e966b-c48c-475a-aa12-7bf699136e2f`) → full readiness brief. Workspace scope stays in the URL. These are local test records, not customer data. No Docker, hosted migration, deployment or deletion performed.
- Releases sidebar now returns to history from full detail. Unconnected workspace artifacts are no longer incorrectly labelled personal; a workspace with no connection checks no longer displays `0/0`.
- Additional browser acceptance: saved brief reopened after refresh; switching to the original personal workspace showed only its older upload, not the QA workspace upload. Archiving the QA workspace retained readable evidence and disabled upload; restoring returned it to Active. QA workspace was left restored, with its local fixture record retained. Independent connection/member migration, responsive/keyboard checks across the full journey, and the gate items below remain. Do not equate this verified slice with full Gate B completion.

- B0: provider-neutral organisation ownership and invitation/member management, confirmed commercial caps, and workspace-first connection/resource/query/worker cutover. Switcher/creation and workspace-owned uploads are integrated; current connection API access is transitional, not the final independent workspace authorization authority. No auth provider enabled.
- B1/B7: finish aggregate daily Overview counters/links and independent-workspace unavailable states; package-only transition is verified above.
- B2/B3: complete GitHub chooser and website ownership/connection recovery parity, permissions and expired-trial messaging; verify source health/pause/remove/reconnect against APIs.
- B4/B6: full uploaded detail parity, filters/pagination, public proof issuer/revoked/mismatch states and sharing/revocation controls; verify redacted projection.
- B5/B8: test independent assignment/status interactions, mutation/activity recovery, roles and settings save/errors; no fake enterprise identity controls.
- Browser desktop/mobile/keyboard verification and end-to-end persisted workflows. Unit tests alone do not close Gate B.

Do not replace this list with a completion claim until each remaining acceptance check has evidence. No push or external deployment performed.
