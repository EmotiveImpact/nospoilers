# Gate B completion audit

USER-REQUESTED PAUSE (6 September, 06:57): finished the current cadence/freshness checkpoint before pausing.38 focused tests and52 source/backend tests pass; corrected-import typecheck and diff validation pass (run73667). No verification run remains pending. Gate B is incomplete; do not mark complete or blocked. Resume with remaining Coverage lifecycle/scheduling parity and rendered verification of the new cadence fields, then the full requirement matrix below. Latest full run remains83464 (806/814, with failures addressed in documented targeted checks), not a clean final acceptance run. No push/deployment or provider activation performed. Local server left available.

B3 cadence/freshness implementation (6 September, 06:57): authenticated repo/package/origin/custody list responses now include configured cadence, server-evaluated check freshness, and explicitly null next-dispatch time. Connected website/custody cadence respects their hour-bucket deduplication; no worker availability is claimed. Coverage shows cadence/unknown/delayed states, and delayed checks remain in the attention filter even when the saved scan passed. Missing/invalid/future check times remain unknown.38 focused tests pass; typecheck caught two test import extensions, corrected and rechecking. Source/backend suites run16088 pending. This does not establish exact scheduling/worker liveness or complete B3 lifecycle parity. Named connection lookup and workspace-management links passed36 focused tests in prior run63064; match is by authenticated installation identity only.

B3 connection identity (6 September, 06:53): normalized source view models now retain actual installation_id/installationId from authenticated repository/package/origin/custody responses, and Coverage detail displays the connection identifier (explicitly unavailable when absent). No account owner is inferred from a hostname, package name or currently selected connection. A four-source matrix proves distinct IDs remain distinct. Run82234 passes35 tests plus typecheck/diff. Source-detail rendered test63260 also passed previously. Named connection/account presentation and workspace management links can use authenticated installation metadata next; cadence/next-check/stale evidence remain separate unfinished B3 requirements.

B3 connected source evidence presentation (6 September, 06:52): Coverage view models now preserve existing API `last_scanned_at` separately from `last_checked_at` for packages/websites. Detail labels registry metadata explicitly, shows missing scan times honestly and warns that check timestamps do not prove scan success. All four source kinds have bounded scope explanations; no invented ownership/schedule values were added. Watch desk33/33 plus typecheck/diff pass in run83001. Dedicated rendered source-detail test added; result recorded separately. Ownership, cadence/next-check and stale monitoring remain open B3 work, not closed by this field correction.

Mobile scan entry and regression follow-up (6 September, 06:50): actual390px header exposes named Search and New scan; clicking header New scan retains QA workspace in `/watch/scan`. Document width390 and four header controls48px wide fit without overlap. Restored1200×868 afterward. Website next-step copy now states supported same-origin assets and scan limits, with monitoring explicitly separate. Permission-enabled run68003 passed37/37 setup/policy/receipt tests; prior shell/architecture/typecheck run31266 passed14/14. Full83464 still recorded as806/814 with targeted follow-ups, not relabelled as clean. No full suite currently running; batch remaining source/inventory changes before another full acceptance run.

Bounded full run83464 terminal (6 September, 06:48):806/814 passed, seven CLI/local-socket failures plus one stale architecture-copy assertion; two EPERM local-server asynchronous errors. Reproduced tsx IPC listen EPERM before scanner execution. Permission-enabled fixture run68487 passes30/30; setup/policy/receipt permission-enabled run68003 pending. Updated stale architecture expectation to require supported checks/findings/limitations and prohibit the removed all-categories claim. Mobile header now names Search independently of hidden text and keeps an accessible compact New scan button visible. Focused shell/architecture/typecheck run started. This remains a failed full run with subsequent targeted corrections, not a clean full pass.

Live narrow policy acceptance (6 September, 06:46): rejected QA exception survives direct reload with both UTC expiry displays and retained decision history. At390×844, document width390 and no overflowing section/article/button/input were observed. Mobile Watch navigation opens as a named dialog and shows the selected QA workspace plus product/settings links. Confirmed accessibility gap: header Search icon button has no accessible name below640px because both text spans are hidden; add an always-present aria-label after the current full run, with a shell regression. Mobile global New scan discoverability also needs checking (desktop header action is hidden at this width). Restored browser to1200×868 after checking navigation to Releases. These are scoped checks, not full responsive sign-off.

B3 scheduling evidence/design (6 September, 06:44): `poller.ts` drives connected GitHub visibility, npm metadata, connected website and custody checks from configured `pollIntervalMs`; website/custody use hour-bucket deduplication, while independent website schedules have separate persisted due times. `/api/health` exposes the configured polling interval, not a per-source exact next execution time or worker heartbeat. Coverage must distinguish metadata checks from successful artifact scans (`last_checked_at` versus `last_scanned_at`), show connection/workspace owner and exact scope, and avoid deriving a guaranteed next scan from a last-result timestamp. Implement truthful scheduler fields through authenticated source responses, with unknown/delayed states and tests for missing scheduling evidence; do not hardcode hourly or imply a failed/inconclusive result is the last successful check. Current bounded regression83464 is still running; notification suite18/18 now passes, not yet a full pass.

Regression39164 terminal (6 September, 06:42): 635/809 passed, 174 failed, four asynchronous errors under default worker concurrency/five-second tests. JSON report retains generic STACK_TRACE_ERROR for many failures, so not all failures are classified as timeouts. Controlled rerun23893 of web-origin plus Watch desk passed56/56 at two workers/30-second budget. Coverage now allows only explicit `passed` to become OK; inconclusive/error require attention and unknown statuses remain unknown across package/website/custody (five matrix cases). Test configuration now bounds concurrent WASM databases to two with finite30-second test budgets; assertions and product deadlines are unchanged. A new full run is required to distinguish timing pressure from remaining functional failures.

Read-only B3 follow-up (6 September, 06:41): confirmed `src/watch/view-models.ts` scanAttention defaults every non-fail/non-queue status to `ok`; real map-custody and website workers emit `inconclusive`. This can suppress attention and present inconclusive evidence as OK. Next source change after live regression39164 terminates: explicit successful-status allowlist, inconclusive/error attention, unknown fallback, with package/website/custody matrix tests. Coverage detail also lacks owner/cadence/next-check fields; those broader B3 requirements remain open and must be backed by real scheduling facts, not invented values.

Verification status (6 September, 06:40): build/lint/diff run86711 completed successfully, retaining the main bundle warning (506.71kB) and existing React warnings. Full regression run39164 remains live; preserve the source snapshot until its terminal result. No clean full run or Gate B completion is claimed yet.

Alert recheck acceptance correction (6 September, 06:38): source rechecks previously used list fallback data even when the selected detail failed; they now require successfully loaded selected evidence, with explicit retry guidance. Website recheck also explains read-only/archived access rather than leaving an unexplained disabled button. Five focused Alerts/rendered/recheck tests pass in run49444. Broad unchanged-source regression started with report `/private/tmp/nospoilers-gate-b-review-full.json`; source/test edits paused until it terminates. Build/lint/diff verification also started; no pending run is counted as passed.

Live B5 scoped request/rejection acceptance (6 September, 06:37): QA request `b84b7234-9da3-476c-8f6d-9b65e43caad4` was created from saved QA attempt `848e966b-c48c-475a-aa12-7bf699136e2f`, exact MAP-003/index.js artifact scope, with a QA-only justification. Reviewed through the real Policy screen and rejected with an explanation. Browser confirms rejected status, preserved request/rejection events and no further decision available; no exception was approved. Local browser session required renewed development login. Expiry input now explains end-of-day UTC, review dates explicitly display UTC, and selected detail carries status/expiry even off-page. Three focused request/review UI tests and typecheck pass in run35956; diff validation passes. This does not close independent-approval/expiry or mixed-source acceptance requirements.

Live B2/B4 upload acceptance (6 September, 06:28): switched via the workspace selector to existing `Gate B local QA` (`da57be82-c929-4f09-8b5e-25d7a13a5e2d`), used global New Scan and uploaded repository fixture `fixtures/clean.tgz` through the visible file chooser. Real queued processing persisted upload `9617a550-1aba-4a4f-a074-356733651b87`, displayed Policy passed and enabled the signed-record action. This consumed one local QA scan allowance; no provider, publication or customer workspace was changed. Selecting existing older sourcemap attempt `848e966b-c48c-475a-aa12-7bf699136e2f` updated URL and mini-preview to Hold/three findings, retaining both attempts. Current-navigation console zero errors/warnings. The hidden input itself intercepts clicks as expected for sr-only; clicking its visible label works. Remaining acceptance includes cancellation/error/permissions and other source types, not inferred from this passing artifact journey.

Live B2 mode entry (6 September, 06:26): authenticated browser switched proof→GitHub→website via the actual New Scan tabs. All retained workspace identity and `/watch/scan` shell; GitHub presents administrator authorisation guidance, website presents ownership entry, and proof stays separate from scanning. No provider authorisation or website scan was initiated. Console remains zero errors/warnings. Corrected website copy that asked signed-in users to sign in again, and made monitoring a separate opt-in rather than implying ownership verification starts it automatically.

Live B4/B2 navigation evidence (6 September, 06:24): in authenticated Playwright, selected Secrets on the existing sourcemap release, reloaded that exact URL, observed Secrets selected with its honest empty category, then browser Back restored All findings on the same upload. “Verify a downloaded record” navigated to `/watch/scan?workspace=…&mode=receipt` inside the product sidebar/header, not the public website shell. No scan or publication was submitted. This verifies this artifact/category/history/verification-entry journey only.

Live browser harness (6 September, 06:23): restarted the identified Vite process with existing `NOSPOILERS_LOCAL_REVIEW=1`, bound to127.0.0.1; current server session24536. No production auth code/provider settings changed. The separate Playwright browser signed in through `/api/auth/development`, resolved the existing workspace, rendered Overview, and clicked its persisted sourcemap attempt into the exact release-detail URL. Current-navigation console reports zero errors/warnings. Dev-server verification skill's agent-browser binary was absent, so the available Playwright connector supplied the fallback checks. Screenshot artifact `gate-b-release-live.png` was captured by that connector; not yet visually inspected. Continue real click/reload/back-forward acceptance using this authenticated browser; no fixture data has been inserted into customer records.

B1 connected diagnostics terminology (6 September, 06:20): Setup no longer calls its five capability counters “all leak paths covered” or implies every capability is mandatory. Headline, description, accessible counter and missing-evidence copy distinguish optional setup from recorded, scoped checks. Existing routing/controls remain intact; no capability was removed. “Nothing connected” was also corrected to “No capability evidence recorded” when evidence is absent, since connection and successful checking are different states.

B4 website retry parity (6 September, 06:19): release mini-preview now routes failed/completed website attempts to workspace website controls, matching full-detail behaviour, instead of offering artifact upload. Artifact retry remains in the scoped New Scan flow. Fifteen preview/detail UI tests pass, including the exact website target route; typecheck/diff pass.

B3 website scope presentation (6 September, 06:18): website cards expose the exact configured origin URL, workspace management and a clear recorded-assets/not-whole-domain scope limitation beside connection health. Six website UI tests pass, including configured-path scope and verified-but-unchecked status; diff passes. Cadence, next due check, pause/resume/disconnect/reconnect and exact attempt links already exist. This does not prove inventory parity for other source types.

Activity rendered acceptance (6 September, 06:16): actual Alerts components now have a passing fixture-backed test for Older/Latest navigation, URL-restored events, cursor clearing on queue change and recovery from an invalid cursor. Fixed Latest activity being disabled during detail failure; it remains usable to return to the current page. Two populated integration tests pass in run2644 and diff validation passes. This establishes component/router-prop behaviour, not a live browser back-button trace.

B5 long-history correction (6 September, 06:15): workspace alert detail now returns the latest50 response events in chronological order with an older cursor; cursor ownership is checked against the exact workspace/alert. UI exposes Older/Latest activity, preserves eventBefore in the URL and clears it when switching alert/status/owner or after a response. Nothing is deleted or truncated in storage. Six response/API/UI regressions pass plus a real120-event pagination test proving 50/50/20 traversal without duplicates and rejecting another alert's cursor; typecheck/diff pass. Explicit rendered pagination/history back-forward acceptance still required.

B1/B7 website preparation correction (6 September, 06:13): Overview API now counts unverified and verified-but-unchecked website health states. First Proof retains four circles but gives existing websites an ownership-verification or first-check action; the second circle is current only for verified unchecked sources. Paused/disconnected/older API states use neutral review guidance rather than claiming readiness. Next-action selection now links attention/delayed website health to filtered Coverage after scan/response priorities. Removed the blanket claim that every relevant exposure check is selected automatically. Nineteen focused API/UI/transition/next-action tests pass in run6749, plus typecheck/diff. Mixed-source browser acceptance and the broader ledger remain open.

Regression terminal result (6 September, 06:11): run45217 finished **802/803**. Its sole failure was `watch-architecture.test.ts` rejecting the newly added text arrow in PolicyScreen. Removed the arrow without changing navigation; all13 architecture tests pass in focused run23716. Build11409 passed before this text-only correction. Report: `/private/tmp/nospoilers-gate-b-acceptance-full.json`. This is not recorded as a clean full run; it is a full run with one subsequently corrected convention regression. Source is no longer frozen; proceed to the documented B1/B7 correction.

Active unchanged-source verification (6 September, 06:04): full run45217 is live, report target `/private/tmp/nospoilers-gate-b-acceptance-full.json`; production build11409 passes with the existing >500kB main-chunk warning (506.56kB). Do not edit source/test modules until run45217 terminates.

Confirmed next B1/B7 work from read-only review: `ArtifactOverview` renders First Proof whenever there are no attempts/alerts/hosted evidence/jobs even if websiteCoverage.total is positive; `WatchFirstProofOverview` always marks step1 active and says connect. A connected-but-unscanned website therefore gets contradictory onboarding. `overviewNextAction` does not consume websiteCoverage at all, so delayed/attention monitoring has no next-action priority. Correct these against actual verified/unverified source state after the regression; keep an unknown/incomplete check from implying a clean result. First Proof copy also says every relevant check is automatically selected despite explicit scoped-check limitations elsewhere.

Workspace access-state follow-up (6 September, 06:03): switcher refresh failures now discard cached workspace choices, disable selection and expose an announced error with explicit retry. A missing selected workspace no longer lets the native select imply another workspace is selected. Five workspace-management UI tests pass, including failure/recovery and no persistent loading option; typecheck and diff pass. This is UI resilience, not a replacement for server membership enforcement.

Live local browser acceptance (6 September, 06:02): opened the existing independent workspace's saved upload `70fb03ba-071a-4a78-92a2-f059e3abe0ba` directly with `uploadView=detail`. After loading, the actual authenticated shell rendered sourcemap.tgz, three findings, engine/time/policy scope, exception-request entry, signed-record action and private sharing preview. This establishes direct-link restoration for this existing failed-policy artifact, not other source/state or browser-history cases. No scan, publication or deletion was performed. The browser exposed a persistent loading placeholder in WorkspaceSwitcher; corrected loaded/empty labels and malformed list rejection.

Populated Alerts component acceptance (6 September, 06:00): `workspace-alerts-rendered.test.tsx` renders the actual `WorkspaceAlerts` and `WatchAlertsWorkspace` components (no presentation stub), loads a website finding, verifies response enablement after detail, requires a resolution note, preserves the exact saved-attempt URL, submits resolve and navigates to Resolved without triggering a scan. Run13186 passes. Network responses are test fixtures; this proves component integration, not live browser/API end-to-end or responsive visual acceptance.

## Alert/policy acceptance correction — 6 September, 05:59 local

Independent website alerts are implemented, not a missing feature: `upload-worker.ts` atomically publishes one alert per immutable finding-bearing website attempt and enqueues workspace notifications. Workspace-scoped readers, response endpoints, assignment, history and UI exist. Fresh run26370 passes website worker, HTTP access/response and UI tests (4 tests/3 files). The UI now requires successfully loaded selected detail before enabling or submitting response actions; failed detail cannot retain an actionable feed fallback. Typecheck/diff pass. Full populated browser acceptance remains outstanding; the transport test still stubs the shared visual component.

Policy UI now explains connected saved-finding requests, approval enforcement and retained historical allowlists. Direct-create errors are shown beside the form and scoped to the installation, with a scope-preserving saved-release link. Four focused policy/request/authority tests passed in run66154 with typecheck. React review guidance informed local error state and scope isolation. These changes do not close B5/B8.

## Verification checkpoint — 6 September, 05:56 local

Full unchanged-source run68059 completed successfully: **797 tests / 180 files**, 461.75 seconds, report `/private/tmp/nospoilers-gate-b-snapshot-full.json`. This verifies the policy snapshot and legacy approval enforcement baseline, superseding the pending-run notes below. It does not close the broader rendered acceptance ledger.

Subsequent B6 correction adds structured malformed/missing-signature/unsupported-version/unrecognized-signature/inconsistent/artifact-mismatch outcomes through the verification API, and distinguishes authentic signature with mismatched bytes in the receipt-input UI. Latest-selection cancellation prevents stale responses and aborts on unmount. All 21 focused receipt/UI tests pass in run41140 with subprocess permission; typecheck and diff validation pass. The first sandboxed attempt failed two CLI exit assertions while the other 19 checks passed; the unchanged permission-correct rerun passed both. No full regression is claimed for these subsequent changes yet.

## Active evidence review — 6 September, 05:47 local

Receipt-input race found during the same read-only review: `ReceiptVerifyPanel.run` has no request identity or abort guard. Choosing a second receipt/pack while the first hash/request is running can let the older result overwrite the newer selection. Add deferred-response tests proving latest-selection wins and unmount is safe, alongside structured outcome presentation. Existing public `VerifyPage` token-race tests cover a different component and do not prove this upload-panel invariant.

Confirmed B6 presentation gap: `ScanPage.verifyHeadline` maps **every** `ok:false` result to “This instance did not sign that release proof.” Yet `verifyReceipt` checks the signature first and can then reject an artifact digest mismatch. The current headline is therefore false for a correctly signed receipt paired with different bytes. Malformed JSON and unsupported versions are likewise not proof of a foreign signer. Next correction needs structured verification outcome codes propagated through the API and rendered separately, retaining existing reason strings/CLI compatibility; test authentic digest mismatch versus invalid signature, malformed input and unsupported version. With HMAC-only receipts, an unrecognized signer cannot reliably be distinguished from tampering; use honest uncertainty instead of inventing issuer identity. Do not modify these source files during full run68059.

Full current-source regression68059 is live with one worker/local fixture permission; hosted.test.ts has passed all58 checks. Do not interpret that partial result as a full pass. Snapshot and legacy-approval changes are recorded in GATE-B-WORKSPACE-EXCEPTIONS.md.

B6 evidence distinctions: `public-verification-ui.test.tsx` covers missing links, non-enumerating private/withdrawn links, transient retry, mismatch precedence, stale-token clearing and malformed response. `upload-proof-api.test.ts` covers authenticated explicit publication, mutation-origin checks, redaction, revocation, membership removal and zero jobs from public reads. These tests do **not** establish issuer taxonomy for uploaded JSON verification. `VerifyPage` deliberately presents recorded metadata rather than claiming signature validation; `ScanPage`/`receipt.ts` handle actual signature checks. Final acceptance must inspect both surfaces and preserve anonymous revoked/unknown indistinguishability rather than leaking the prior existence of a private proof. B6 remains open until those distinct requirements have matching evidence.

Checkpoint114 (B4/B5 dependency): new hosted scans now preserve full immutable private reports, atomically with receipt/revision persistence. A release-authorized no-store evidence API exists; older missing reports are explicitly unavailable. Nine focused evidence/access/rollback/ledger/connected-upload tests pass. Hosted-finding request/UI integration remains unfinished; no row is closed on this evidence-storage checkpoint.

Checkpoint113: connected uploaded findings now enter the shared scoped request/independent-approval/revocation flow and hosted policy applies only matching approved connection/digest exceptions. Three real backend/migration tests and nine request/detail/review UI tests pass; typecheck passes. This extends B5 beyond independent uploads but does not close it: ordinary hosted evidence without an upload attempt, legacy direct allowlists, snapshot timing and populated browser acceptance remain open. Full788 verification predates112/113.

Connected exception checkpoint112: immutable legacy workspace/scope backfill and protected revocation are implemented. Two ownership/authority tests plus 12 existing policy, connection-placement and independent-exception compatibility tests pass; typecheck/diff pass. No full-suite pass is claimed for112. Legacy creation approval and unified review remain open under B5; this checkpoint does not close that row.

Latest full verification (6 September): run42836 passed 788 tests / 173 files in 265.19s against unchanged source; report `/private/tmp/nospoilers-gate-b-exceptions-full.json`. Production build97812 also passes, retaining the existing 504.59kB main-bundle warning. This supersedes the pending exception-suite notes below. Local browser confirms the independent policy screen, scope explanation, approval setting and empty exception history; populated review acceptance remains unverified. A subsequent legacy revocation authority correction is being verified separately and is not included in that full-suite result.

Current implementation checkpoint (111, 6 September): scoped independent exceptions now have request/review/decision APIs and UI, exact artifact or website scope, expiry, live independent-approval checks, periodic detail refresh and immutable history. Both workspace shells expose independent policy/reviews while preserving connected GitHub controls. A real mixed-workspace worker test confirms policy separation. Focused tests/typecheck/diff pass; the full exception regression is running, not yet accepted. This supersedes older statements that independent exceptions are wholly missing, but does not close B5 or the broader ledger. Connected legacy exception migration and rendered workflow acceptance remain required. Detailed evidence is in GATE-B-WORKSPACE-EXCEPTIONS.md.

Latest unchanged-source verification: run91327 passed783 tests across170files in246.51s, report `/private/tmp/nospoilers-gate-b-notifications-full.json`. This includes migrations through109, independent scan tokens, notification outbox/transport/runtime/API/UI and the old email-history remigration correction. Production build32813/typecheck/diff pass with the existing main bundle warning. These supersede older pending/current-suite statements below. B0–B8 remain open against their complete workflow requirements; next confirmed implementation gap is scoped workspace exceptions/approval, documented in GATE-B-WORKSPACE-EXCEPTIONS.md. Browser evidence for notifications is the real empty state only, not populated/mobile acceptance.

6 September verification checkpoint: unchanged-tree run19611 passed757 tests in153files (`/private/tmp/nospoilers-gate-b-current-full.json`). It includes migration101 workspace Alerts and current Overview activity/deep-link changes. Subsequent workspace token revocation authority correction has three focused passing tests and typecheck/diff validation. These results supersede earlier missing/unclean suite notes below; B0–B8 remain open where full workflows or rendered acceptance are incomplete.

This is a requirements ledger, not a declaration of completion. The goal is the whole Gate B in ENTERPRISE-READINESS-PLAN.md, including approved workspace migration and MOCKUP-PRODUCTION-CONTRACT.md state acceptance. Historical passing suites are baselines only. Auth-provider activation, physical monorepo move, Docker/hosted isolation, live deployment and enterprise SSO remain separately deferred gates; do not silently count their absence as implemented.

Latest evidence (100): independent website connect/verify/check/result and retained lifecycle controls exist, plus opt-in scheduling with atomic shared-budget admission and worker revalidation. Stable alert assignment is implemented (094), not yet fully accepted. Overview now distinguishes website/artifact attempts. Rows below remain open until their broader scope is verified; older “not implemented” subitems should be read alongside these checkpoints. No current clean full-suite result is claimed; the previous /private/tmp report was absent after restart.

| Requirement | Current evidence | Remaining proof/work | Status |
| --- | --- | --- | --- |
| B0 identity/workspaces | Migrations 071–100; independent members/billing, fresh GitHub binding, website ownership, stable alert assignment | Complete remaining source/resource migration and mixed-source acceptance; provider activation remains deferred | Open |
| B1 First Proof/setup | Four-circle empty state and artifact-to-overview transition | Evidence-driven intermediate states, consolidated diagnostics, browser acceptance | Open |
| B2 New Scan | Durable uploads/login claim; GitHub intent/binding; independent website challenge, verified admission and saved result tests | Complete source-type state matrix, browser journeys and contextual setup consistency | Open |
| B3 Coverage | Independent website lifecycle, schedule, health, filtered links and mixed-workspace entry; retained repository disconnection | Full inventory parity across source types, ownership/scope presentation and rendered acceptance | Open |
| B4 Releases/detail | Saved selection, preview/detail, pagination, immutable uploaded evidence | Full source/type/state parity and browser reload/back-forward verification | Open |
| B5 Alerts | Independent website publication, workspace HTTP responses/history, stable assignment, pagination; immutable related releases; scoped exception/snapshot enforcement | Populated rendered response/exception/expiry workflows and mixed-source browser acceptance | Open |
| B6 Proof | Private records and explicit redacted publication/revocation | Verify issuer/invalid/revoked/mismatch taxonomy across relevant proof types; no scan-job side effects | Open |
| B7 Daily Overview | Workspace saved-scan totals, website health totals/filter links, corrected mixed-workspace first-proof transition | Connected Overview aggregation, independent exposure alerts, intermediate first-proof states and browser acceptance | Open |
| B8 Settings | Real team/policy/retention/audit; independent tokens, notifications/outbox/transport and connected APIs | Rendered save/error/audit and permission acceptance across workspace types; approved lifecycle boundary | Open |
| Cross-cutting acceptance | Unchanged-tree full run 81558 passed 740 tests / 144 files; build/typecheck/diff pass, existing lint/bundle warnings | Remaining scoped journeys, keyboard/responsive/browser matrix and missing workflows; no completion inferred from test count | Open |

Keep the original requirements intact. Closing a row requires authoritative code, relevant passing checks and rendered workflow evidence where required. A disabled control does not fulfill an explicitly required workflow. No percentage substitutes for this ledger.

Current execution: complete GitHub start → verified callback/webhook → source binding → repository sync/first scan → Coverage/Releases before independent website/registry/settings integration. Preserve existing evidence and payers. Do not move populated sources between tenants.

Latest evidence: fresh GitHub binding now stages signed deliveries and reconciles them against the complete current repository inventory before initial checks. Coverage is reachable in artifact-only workspaces, with explicit GitHub connection and archived/member restrictions; local browser confirmed rendered navigation/content. Typecheck and 10 focused connection/Coverage tests pass. Full regression run is in progress (session 4418; report /private/tmp/nospoilers-github-coverage-full.json). Do not treat that pending run as passed. Next inspect normal repository removal/reconnect retention and the connected chooser, then independent website/settings parity. No rows closed from these focused checks alone.

Superseding checkpoint: run 4418 completed 703/706, three regressions fixed (connected health fixtures and proof-sharing audit constraint). Migrations now through 093. Repository disconnection retains records, stops work and prevents incidental/cross-tenant reactivation. Coverage now includes a separately scoped retained-repository section and explicit GitHub access guidance; active scanning APIs continue to exclude disconnected repositories. Three focused retention/UI checks plus typecheck/diff pass. Full rerun session 5161 remains in progress; its report is /private/tmp/nospoilers-repository-retention-full.json. That suite started before the newest retained-section changes, which have separate targeted checks. Next substantial B5 gap confirmed: alert assignment persists login strings rather than stable internal member IDs; workspace member authority already feeds the lookup, but stable identity, chooser and transactional revalidation remain necessary.

Run 5161 is now terminal: 706/707. The sole failure loaded the newly edited retention test against an earlier cached Store module (missing listDisconnectedReposForUser); the matching fresh focused run passes all three tests. This is not a clean full-suite pass: rerun against an unchanged worktree before final acceptance. Build/typecheck/diff pass; existing >500 kB bundle warning remains. Do not edit test/source modules while a full verification run is in flight.

B5 identity implementation now exists through migration 094: stable member IDs, protected member-list API, chooser, transactional authority recheck, automated routing identity and current-name projection with preserved historical snapshots. Five focused tests pass along with typecheck/diff. This does not close B5: full HTTP, rename/mine counts, routing, unassignment and response-state/browser acceptance still require proof.
