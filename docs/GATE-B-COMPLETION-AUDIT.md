# Gate B completion audit

## Gate B complete — 8 September 2026

The original B0–B8 product-flow scope is accepted. `GATE-B-FINAL-EVIDENCE-MATRIX.md` maps each requirement to implementation and complementary real API/database, rendered-state and browser evidence. The final review confirms workspace identity/limits, First Proof, scan intake and states, retained Coverage, stable release history/detail, response/exceptions, private proof, scoped Overview and settings feedback/permissions. Historical Open entries below are superseded by this closure, not an unfinished feature backlog.

Final unchanged-source verification: session **38375 passed 986 tests across 198 files**, 268.84 seconds, including explicit staged-expiry rejection. Build **27665 passed**, including typecheck. Lint exits 0 with existing warnings; diff validation passes. Two tar locale warnings and the 501.17 kB main bundle warning remain non-fatal. No verification process from this checkpoint remains active.

Browser acceptance uses real application APIs in disposable local data: independent first login, mixed connections and exact-source count navigation, owner/admin/viewer restrictions, alert response and independent exception approval, explicit redacted sharing/revocation, mobile selection/detail/Back, real file upload admission to a queued attempt, and policy save confirmation/revision history. Deterministic transport/failure/expiry assertions supplement these journeys; this is not a claim of exhaustive browser permutations or live-provider execution.

Gate B completion is not whole-application launch readiness. Deferred worker/isolation and live-provider/hosting validation, broader Gate C visual/accessibility work and later enterprise capabilities remain governed by their existing gates. No deployment, provider activation, Docker, production migration, push or customer-history deletion was performed in this closure. Changes remain in the local working branch; `backup89` is untouched.

## Explicit staged-expiry acceptance — 8 September 2026

Extended the real acquisition API/database test to expire staged bytes before authenticated claim. The endpoint returns 404 with the expired-state explanation; saved attempts, jobs and personal usage are unchanged. The complete acquisition file passes (2 tests, session 73899). This adds assertions only, not production behavior. It postdates the 986-test regression, so the final integrated record must distinguish the snapshots.

## Final combined regression — 8 September 2026

Real B8 browser save: in the disposable mixed workspace, enabled strict independent scan policy and saved through the production API. The page returned “Policy saved for subsequent scan starts,” advanced revision 1 to 2, disabled the unchanged Save button, and expanded history showed both revisions with actor `qa-owner` and timestamps. Independent approval remained enabled. This changed only ephemeral QA policy, not customer policy or previous scan evidence.

Session 59716 completed with exit 0: **986 tests across 198 files passed**, duration 271.79 seconds. Source and tests stayed unchanged throughout. This includes the latest marketing-menu tests as well as the Gate B implementation. Two tar locale warnings were non-fatal. Production build 19524 passed against this source; the main bundle size warning remains. No test or build process from this checkpoint is still running.

## Real mobile upload admission — 8 September 2026

Using the visible drop-zone file chooser at 390 px, uploaded the repository's disposable `fixtures/clean.tgz` through the real QA API. The server admitted saved attempt `df7ac42a-1589-4501-94f7-19671fa0d23b`; the UI navigated to its exact workspace/installation/upload URL and selected its preview. It displays Queued and explains that the same record can be revisited. Earlier website and artifact findings remain in history. The fixture starts no worker, so this is admission/navigation/queued-state evidence, not a completed worker scan. The hidden file input was not directly pointer-clickable; its visible labelled drop zone correctly opened the native chooser.

## Mobile navigation and current build checkpoint — 8 September 2026

Further mobile B4 acceptance: selected the independent artifact instead of the initially selected website attempt. The decision preview changed from retrieved website assets to packed artifact scope and wrote the exact upload ID to the URL. Opening its full brief retained workspace/connection/upload identity and showed the artifact heading and findings. Browser Back restored the artifact's pressed selection and preview, independently of the connected repository revision selection. The full brief also measured 390 px document width at a 390 px viewport. No scan or evidence mutation was performed.

At 390 × 844, the populated disposable mixed-workspace Overview has a 390 px document width (no horizontal overflow). Its mobile Watch navigation opens as a labelled dialog with the real workspace-scoped product links. Selecting Releases navigates to the same workspace and primary connection. This is real API-backed browser acceptance, not a component mock; it does not yet establish every mobile detail state.

Production build session 19524 passed after the latest marketing mobile/menu styling changes. The main bundle is 501.17 kB with the existing size warning; no build error. `git diff --check` also passes. The earlier 983-test product regression remains a separate unchanged-source snapshot, as recorded below.

## Integrated verification checkpoint — 8 September 2026, 16:42

Full regression run39178 passed **983 tests across197 files**,281.58 seconds, against unchanged source. The prior restricted run66493 passed976 and failed7 with local IPC/loopback EPERM errors; an independent CLI reproduction confirmed the blocked socket. The successful rerun used approved local-process permissions, not weaker tests. Build18791 passed with the existing500kB bundle warning. Later marketing navigation/mobile styling changes are outside that product snapshot and have their own focused tests/typecheck; they are not silently included in the983 claim.

Latest product corrections included mixed-source First Proof guidance (one paused/unavailable source must not hide another usable source), truthful “source revision” labels rather than calling package versions/tags commits, and viewer scan-report guidance that explains read-only access rather than suggesting renewal. Focused regressions cover each correction.

Additional real-browser QA: an empty independent workspace renders the approved four-circle first-login journey without sample results. A second administrator approved the owner's bounded exception after entering a decision explanation; the API-backed page recorded the reviewer, timestamp, exact artifact digest and expiry. The original upload still displayed its three unsuppressed findings afterward. Sharing that disposable upload displayed the redaction preview, then created a loopback-only public link. An unauthenticated GET returned only digest, scan date, decision, count/severity and assurance text—no filename, finding paths or receipt. Revoking through the UI made the same unauthenticated URL return404. No customer data or external provider was changed.

Gate B completion is not inferred solely from this regression. Remaining closure work is the final requirement-by-requirement acceptance matrix and any explicitly uncovered navigation/responsive checks; deferred live-provider/hosting and enterprise gates remain separate.

## Populated browser acceptance — 8 September 2026

Follow-up with correctly seeded production membership: delayed Coverage now contains only its matching package, not the unrelated repository/website. Its row opens connection9902/source npm1 and displays that package's stored digest, failed policy, delayed metadata freshness, cadence and latest brief. The secondary connection's Needs review revision opens release2 with its exact three findings; it does not substitute the primary connection's passing release. Viewer Team lists members/access history without invitation or role-write controls. Viewer New Scan stays in the product shell and explicitly disables scan access as read-only. A billing403 is expected for that viewer and does not expose billing controls. Remaining browser acceptance is tracked separately; these checks do not assert the full gate complete.

An isolated QA server (`scripts/gate-b-browser-fixture.ts`, localhost4359) uses real application APIs and ephemeral PGlite with no workers or outbound provider calls. Its explicitly disposable workspace contains two connections, repository/package revisions, an independent artifact, website attempt and alerts. This does not modify the normal local-review database.

Browser verification confirms Overview's delayed count opens workspace-wide monitoring results, and the package result generates the exact secondary connection/source URL. The result screen incorrectly also displayed unrelated current-connection inventory: a focused-view correction is in progress. Secondary connection access is currently rejected by the workspace boundary; investigate the fixture's raw connection binding and missing membership synchronization before attributing this to production authorization.

The primary connection's Policy passed list shows its one saved repository revision. Opening it displays that exact revision's brief, digest, recorded check scope, missing identity/delivery evidence and retained findings. Workspace and connection remain in its detail URL; no scan was submitted and the desktop page has no horizontal overflow. These observations do not yet close mixed-source/role acceptance or the full gate.

The real Assigned to me filter returned only the seeded assigned website alert. Acknowledging it through the UI changed Open3/In progress0 to Open2/In progress1, navigated to its stable waiting/alert3 URL and recorded the named actor/timestamp. Its original three findings and saved website-result link remained visible. This mutation affected disposable QA data only. The secondary connection rejection was traced to the fixture bypassing production installation linking; repair the seed using the real store helper before repeating multi-connection acceptance.

## Integrated regression and final flow batch — 8 September 2026

Full run6921 passed **973 tests / 196 files**,281.26 seconds, against unchanged source. It includes atomic website/package/repository publication, repository generation121 and the first docs/marketing shell corrections. Later sidebar/hero refinements and B1/B7 count-navigation changes are not covered by that snapshot. Build80759 passed (existing500kB warning); subsequent B1/B7 authority refinements have focused/typecheck evidence.

B1/B7 implementation now addresses the two concrete gaps from the independent closure review: connected sources without saved evidence receive inspect guidance; connected monitoring counts open workspace-wide health-filtered source results, and hosted decision counts open the correct connection's hosted revisions rather than upload filters. Workspace membership and exact workspace/connection pairing are required on the hosted decision API.25 related tests plus a separately rerun mismatch regression pass; browser acceptance of populated connected counts remains outstanding.

Live local-review browser: Overview Need review1 opens `uploadStatus=attention` in the same workspace; the saved sourcemap artifact opens its stable full brief retaining workspace/filter. Reloading that detail URL and browser Back restore the filtered history. No new scan or customer mutation occurred; desktop pages had no horizontal overflow. This is artifact navigation evidence, not populated multi-source acceptance.

The bounded remaining work is populated connected-source/decision navigation, intermediate First Proof, mixed-source response/settings/permission browser acceptance, and final regression/build on the accepted unchanged implementation. Existing tests establish extensive API/database behavior; neither them nor missing browser evidence justifies rebuilding implemented screens. Live-provider retry/isolation/hosting remains in the previously deferred gates.

## Parallel implementation checkpoint — 8 September 2026

Website and npm observation publication now use explicit transaction helpers. Source metadata, map identity, signed evidence/revision, alert linkage and custody follow-up admission commit together; npm also includes canonical registry delivery linkage. Delivery happens after commit and cannot replace a saved result with an inconclusive result. Real database rollback tests reject alert insertion and prove earlier writes do not survive. Website follow-up acceptance additionally proves an inconclusive result creates a separate revision without modifying the previous result. Website batch: 25 tests passed; package/identity batch: 42 passed plus the dedicated package rollback test. Repository release-asset publication is the next implementation batch, not yet claimed complete here.

Independent integration review: 42 tests across ten files passed (run84508), covering First Proof transition, Overview ledger, proof API/private sharing, verification and sharing UI, release brief/workspace scope, notifications UI and Team UI. These are targeted API/database/rendered checks, not a new full-suite result or complete browser acceptance. Full regression remains reserved for unchanged integrated source after the implementation batches.

PR #42 website/docs is merged and pushed as `1d6fa36`. It is no longer a separate unstarted workstream. Its supporting pages preserve the homepage; 27 website tests and production build passed after browser-test integration corrections. This does not close a Gate B product requirement or operational launch gate.

Remaining implementation/acceptance boundaries stay within B0–B8: repository publication/lifecycle consistency; connected notification delivery parity; mixed-source result/navigation and role/error acceptance; and the final unchanged-source regression/build. Historical Open rows below mean unclosed acceptance, not missing screens to rebuild. Provider activation, Docker/hosted isolation and broader Gate C redesign remain deferred.

## Live Team/Audit separation verified — 8 September 2026

Browser tab13 confirms the corrected real Team route shows “No access changes recorded yet” for the existing personal workspace. Following its scoped Audit log navigation shows the previously observed proof-published/proof-revoked records still present with actor and timestamps. Workspace selection is preserved across both routes. This verifies filtering rather than deletion, without sending invitations, changing access or publishing proof. Audit itself explicitly limits its coverage to recorded workspace-management activity; it is not an exhaustive source/billing event ledger.

## Team access-activity correction — 8 September 2026

Live Team inspection showed proof-sharing events under Access activity. The team endpoint now filters to invitation and membership events before the100-event limit; unrelated workspace history remains stored for Audit log. The UI explains the scope and shows a genuine empty access-history state. A real database test inserts an unrelated proof event, verifies it is excluded from team activity but retained in storage, and retains invitation-revocation assertions. Team/membership tests pass18/2 (run20971), typecheck passes. The initial live observation is confirmed; post-fix browser rendering still needs checking.

## Invitation API/status parity — 8 September 2026

Server inspection confirms expired, revoked and previously accepted invitations return409, not just404/410. The inbox now removes the unavailable entry for409 too. The rendered status matrix and actual membership expiry/revocation/replay tests pass together:18/2 (run36394); diff validation passes. This corrects the earlier mocked status coverage to match the real API contract.

## Invitation inbox states — 8 September 2026

The inbox now distinguishes initial loading, confirmed empty, populated and error states. Retry clears stale entries and shows loading rather than claiming an empty inbox before the response arrives. A deferred-response rendered test verifies the loading-to-empty transition; all10 team UI tests, typecheck and diff pass. This completes the visible inbox-state correction without asserting complete B0/B8 acceptance.

## Invitation acceptance lifecycle — 8 September 2026

Inbox acceptance now has a synchronous duplicate-submit guard, validates the returned workspace before navigation and ignores completion after unmount. Confirmed unavailable invitations (404/410) are removed; access denial clears the inbox until refreshed. Four rendered regressions cover missing workspace, expired/unavailable responses and late success after departure; the existing successful acceptance test remains passing. Team UI and real membership tests pass16/2 (run35876), typecheck/diff pass. No real invitations or accounts changed. An already accepted server membership is not rolled back merely because the client leaves.

## Team permission lifecycle — 8 September 2026

Team writes now use a synchronous submission guard, ignore responses after unmount and invalidate older team reads after successful/denied mutations. Access-denied responses clear editing controls until Retry reloads current authority; inventory arrays are validated before rendering. A rendered denial/retry regression verifies an owner becoming a viewer cannot retain invitation controls. Team UI and real membership tests pass12/2 (run67774); typecheck/diff pass. No real invitation or role change was sent; invitation-inbox acceptance lifecycle remains to review separately.

## UI integration review — 8 September 2026

Applied the React best-practices checklist to the recent scan, GitHub and website lifecycle changes: stable workspace keys, guarded async updates, event-triggered mutations and cleanup of existing polling/submission resources. `npm run lint` exits0 with warnings only (effect-driven state, Fast Refresh export and redundant boolean expression); this is not a warning-free result. Optional effect/architecture refactors are not added as Gate B blockers merely to silence warnings. The recent scan batch build25669 passes with a 508.26 kB main-chunk warning; subsequent GitHub/website changes have focused/typecheck evidence and still require their integration build.

## Website stale-poll regression — 8 September 2026

The website UI race is now exercised explicitly: the initial inventory succeeds, a later poll remains pending, a scan action returns403, then that older poll returns its previously authorized inventory. Source controls and details remain absent and the denied-access message remains visible. Website UI, source wrapper and real independent website scan tests pass11/3 (run95393); typecheck passes. This verifies that precise response ordering without customer mutations or external providers.

## Website mutation permission refresh — 8 September 2026

Website action responses denying access (401/403/404) now invalidate cached inventory and confirmation controls immediately and request a fresh inventory. A load generation prevents an earlier refresh from republishing invalidated rows. The regression denies Scan website, then fails the refresh, verifying old source controls remain absent. Website UI/source-wrapper tests pass9/2 (run56389); typecheck/diff pass. Server authorization remains authoritative; this is truthful frontend revoked-access handling, not new provider activation.

## GitHub connection UI lifecycle — 8 September 2026

Connection-start state is now keyed by workspace. Start and completion handlers ignore responses after their screen unmounts, including before external/local navigation. Rendered regression verifies a pending first-workspace error cannot appear in the newly selected workspace and that its button can start a request scoped to the new destination. GitHub UI, connection API, binding and intent tests pass13/4 (run69143); typecheck/diff pass. No live GitHub installation or provider activation occurred; actual OAuth/provider journey acceptance remains separate.

## Acquisition/submission integration batch — 8 September 2026

All submission callbacks now require either a well-formed staging acknowledgement (target and positive finite expiry) or a confirmed queued upload. Arbitrary immediate JSON is not displayed as completed evidence. Five additional rendered fixture cases reject malformed responses without navigation. Combined submission UI, transport, acquisition, upload workflow and workspace ownership tests pass28/5 (run40040); typecheck passes. This verifies the integrated scan-entry batch following full915, not the full Gate B scope.

## Saved-attempt response validation — 8 September 2026

The staged-claim client now requires a confirmed queued response with a nonempty saved upload ID, rather than treating arbitrary successful JSON as a completed report. The ordinary queued-submission branch shares that validation. Five malformed claim cases (empty, null, false queued, missing ID and empty ID) show recoverable error feedback without navigation. All10 submission UI tests pass; typecheck/diff pass. This is client response-shape hardening, not evidence of complete server workflow acceptance.

## Claim replay and truthful progress — 8 September 2026

Real acquisition coverage now repeats an accepted claim with a different requested workspace, proving it reopens the same upload without changing ownership, usage or job count. Loading copy now says Submitting/waiting for server confirmation rather than claiming the scanner is already running. Acquisition, submission UI and transport tests pass10/3 (run3073); typecheck and diff pass. This verifies retry identity and displayed submission semantics, not all queued-worker progress states.

## Staged claim recovery — 8 September 2026

Failed reveal/claim screens now provide an explicit Retry staged upload action and explain expired-input recovery. The real acquisition regression archives the selected workspace, verifies a denied claim creates no upload or scan, restores it and successfully claims the retained artifact. Rendered coverage verifies 403 feedback followed by an explicit retry and queued release navigation. Combined acquisition/submission tests pass7/2 (run86803); typecheck and diff pass. Retrying an already accepted claim continues through the existing saved-attempt lookup rather than resubmitting new work. Full Gate B acceptance remains open.

## Staged claim destination — 8 September 2026

The post-login reveal effect now passes selected workspace/connection to claim admission through the shared URL builder. Rendered regression proves the scoped POST, while the real acquisition test creates a second workspace, claims the staged artifact there and confirms the personal workspace remains empty. Existing no-anonymous-scan, saved worker evidence and forbidden-user assertions still pass. UI/transport/acquisition run3956 passes9/3; extended database acquisition run7280 passes2 and typecheck/diff pass. The release redirect remains upload-ID-only so canonical ownership resolution handles replay of an already claimed upload without falsely assigning a new workspace in its URL.

## Submission batch acceptance — 8 September 2026

Rendered submission coverage now verifies three outcomes: remaining on the page opens the queued release in the original explicit workspace; changing workspace or leaving the page prevents late navigation. Combined submission UI, upload transport, acquisition and real workspace-upload ownership checks pass10/4 (run40174), with diff validation clean. These focused checks cover the local fixture/destination/navigation batch without rerunning the entire suite. The previously recorded full915 predates this batch.

## Submission navigation lifecycle — 8 September 2026

Scan screen identity now includes both workspace and connection, including URL-only workspace contexts. Completed/failed submission callbacks check that their screen remains mounted before changing state or navigating. A rendered regression clicks a local fixture, confirms its explicit workspace request, switches workspace before completion and verifies the old queued response cannot navigate. Test59263 passes; the initial test harness used an unavailable matcher and was corrected. This closes that specific late-response defect, not cancellation of already accepted server jobs.

## Shared scan destination regression — 8 September 2026

Normal uploads and local fixture requests now share `scanSubmissionUrl`, carrying both explicit workspace and optional installation consistently. The transport test verifies independent, connected and default URL shapes and the real upload transport's selected URL. Upload-transport and real workspace-upload ownership tests pass5/2; typecheck and diff validation pass. This is routing/helper plus backend ownership evidence, not a rendered fixture-click test or a fresh full-suite result.

## Local fixture workspace correction — 8 September 2026

Inspection of the live example library found that its JSON scan request omitted selected workspace/installation parameters, unlike ordinary uploads. The request now forwards both scoped IDs from the screen search to the existing server admission path. The library is explicitly labelled development-only and explains that fixtures create saved queued attempts under scan limits. Backend inspection confirms fixture paths are rejected in production, require opt-in local flags, stay beneath fixtures and enter normal authenticated submission. Acquisition regressions pass2; direct rendered request regression remains to add. This correction follows full915 and is not covered by that earlier snapshot.

## Live saved release acceptance — 8 September 2026

Browser tab11 opened the existing saved artifact from history into its full brief. Verified recorded findings, timestamp, engine, explicit missing custom policy fingerprint, artifact-only scope, immutable-evidence wording and private-by-default redacted sharing preview. Opening Request a policy exception reveals exact rule/path/digest scope, justification and expiry inputs, and disabled submission while incomplete; no request was submitted or proof published. A direct URL selecting this failed-policy artifact with `uploadStatus=passed` preserves the selected evidence, shows no matching list rows and explicitly explains the out-of-filter selection. This closes that specific live filter-conflict check, not multiple-result switching or all-source B4/B5 acceptance.

## Live scan launcher acceptance — 8 September 2026

Local browser tab10 verified `/watch/scan` resolves the existing personal workspace and stays in the product shell across Package, GitHub, Production website and Verify release proof tabs. Each selection updates mode while preserving workspace. Website entry requires an HTTPS origin and shows disabled continuation with no input; it explicitly separates ownership verification from optional monitoring. Proof mode explains signature versus outcome, local optional digest hashing and no new scan; reloading preserves the selected proof mode and workspace. No source was connected, scan submitted, proof uploaded or provider activated. This covers launcher/navigation and initial states only, not successful GitHub/website execution or the complete B2/B6 matrix.

## Full regression — 8 September 2026, 14:57 snapshot

Run5295 completed against unchanged application/test source: **915 tests / 189 files passed**, 247.45 seconds. This includes website connection-generation migration120, worker invalidation, guarded metadata/receipt/alert writes and paused manual/deployment admission. Two tar locale warnings were non-fatal. This supersedes the earlier 915-test snapshot for these changes; it does not close the remaining end-to-end acceptance requirements or imply an atomic combined publication transaction. Use focused tests during the next implementation batch and reserve another full run for its integration checkpoint.

## Paused website admission — 8 September 2026

Manual checks and deployment-token scan requests now reject paused websites with a 409 resume-monitoring message before queue admission. Real-store and HTTP regressions assert the job count does not increase. All 24 website tests pass (run60940). Queue-generation and worker/persistence guards still protect concurrent lifecycle changes after admission; this entry does not claim the overall publication pipeline is atomic or close Gate B.

## Website publication regression checkpoint — 8 September 2026

The website publication test now proves current active connections can save a passing receipt and linked alert, while a paused connection is rejected even with its current generation. Stale-generation rejection also leaves receipt/alert counts unchanged. Combined website, receipt, npm and package-identity run67433 passes 84 tests / 4 files with required CLI subprocess permissions; the restricted run had two CLI failures, not a clean pass. Typecheck, production build54168 and diff validation pass. The existing 506.77 kB main chunk warning remains. This is focused regression evidence, not full-suite or Gate B completion.

## Website evidence publication guard — 8 September 2026

Website receipts and alerts now lock and validate the captured source generation within their respective persistence transactions. Both success and inconclusive worker paths carry the guard. The real-store website regression confirms stale receipt/alert attempts reject without increasing either saved count; all 24 website tests pass. Individual writes are protected, but metadata, receipt and alert are still separate transactions, and external delivery after commit remains a separate boundary. Full Gate B acceptance is not claimed.

## Website persistence guard — 8 September 2026

Connected website freshness, scan-result and map-identity writes now conditionally accept the worker's captured connection generation only while the source remains connected and unpaused. Rejected scan/identity writes stop subsequent worker publication. A real-store regression verifies obsolete writes return false and leave all origin fields unchanged; a current-generation freshness update succeeds. All 24 website tests, typecheck and diff validation pass. This does not yet make receipt, identity and alert publication one atomic transaction; that remaining publication boundary and the broader B0–B8 acceptance remain open. No deployment or production migration occurred.

## Full local regression checkpoint — 8 September 2026

Exact stale website queue regression: an active/resumed origin receives a job carrying its older generation; the real worker completes it without any network fetch. Targeted website test1, typecheck and diff pass. This verifies queue-entry invalidation independently of the paused-entry and mid-crawl checks; atomic persistence remains outstanding.

Website queue producers (initial/manual/poll/deployment) now include origin connection generation; initial/poll/deployment dedup identities include it where applicable. Worker entry rejects a queued generation that no longer matches, refunding unused admission. Legacy generation-less jobs are accepted only for unchanged generation0 origins. Complete website suite24 and typecheck pass. Exact stale-queued-cycle regression and atomic publication guards remain to verify/complete; full915 predates migration120 and this change.

Migration120 adds a database-maintained origin connection generation for pause/disconnect/verification/source-identity changes. Worker boundary rechecks compare the captured generation, detecting a pause/resume cycle even when the origin is active again. Expanded worker regression verifies that exact cycle prevents scanning and preserves the old inconclusive verdict; targeted test1/typecheck/diff pass. Only isolated test databases were migrated in this checkpoint. Queued generation binding and atomic publication remain to implement; full915 predates120.

Connected website worker rechecks current origin pause/disconnection and verification identity after crawl, after scanning, and before handling a failure. Pre-scan cancellation refunds unused work; completed scan work is not refunded. A pause-during-crawl regression verifies no scanner invocation or replacement verdict; targeted test1/typecheck/diff pass. These boundary rechecks do not yet provide atomic multi-record publication or detect a pause/resume cycle that completes between checks.

Connected website pause admission: polling excludes paused origins, and worker entry rejects a previously queued job when the current origin is paused. Origin reads retain explicit pause metadata. Expanded real-worker regression confirms no poll enqueue and zero network fetches for the paused queued job; targeted test1 and typecheck pass. Mid-crawl lifecycle changes and atomic publication still require separate guards; full915 predates this fix.

Connected website Overview freshness now requires a conclusive saved scan status as well as ownership and observation time. Missing/inconclusive status remains unknown; passed and failed-policy can both be recently observed, without implying safety. Expanded real-store state regression and complete website-origin suite pass **25 tests / 2 files**; typecheck passes. This also verifies the unchanged-content recovery correction against existing website admission/refund tests. Full915 predates these corrections.

Connected website recovery: unchanged-content optimization now requires a previous conclusive passed/failed-policy scan. An inconclusive observation cannot indefinitely suppress rescanning identical bytes. Worker regression verifies the ordinary unchanged check still refunds its reservation, then a distinct later job rescans after inconclusive state and records passed. Targeted test1 and typecheck pass; the initial test accidentally reused the hourly dedup key and was corrected to enqueue a distinct later job. Full915 predates this fix.

Connected Overview custody freshness no longer treats an inconclusive or missing custody verdict as a recent verified observation merely because an attempt timestamp exists. Such sources are unknown; conclusive checks retain cadence classification independently of release safety. Real-store regression verifies inconclusive→passed→disconnect behaviour, and typecheck passes. Full915 predates this correction.

**Latest full checkpoint: 915 tests / 189 files pass**, run91431, 237.11 seconds, against unchanged application/test source. Production build36829 passes with the existing 506.77kB main-chunk warning; lint exits0 with warnings, no errors. This supersedes all earlier "latest" full results below, including893, for the settings, routing and verification corrections. Gate B remains unclosed pending the explicitly tracked broader acceptance and consistency requirements; this is regression evidence, not production readiness certification.

Policy save recovery requires reload after an incomplete successful response and removes stale policy controls after 401/403. Neither case claims a saved policy; revision conflict handling remains unchanged. Policy UI4, typecheck and diff pass. Full893 predates these settings changes.

Live current settings smoke acceptance: token → Notifications → Scan policy navigation preserves the local-review workspace and authenticated shell. Empty credential/history pages show disabled pagination; blank token/destination forms cannot submit; Notifications clearly identifies the unconfigured email provider; policy shows saved revision0, disabled unchanged save and empty exceptions with refresh. No credentials, destinations or policy changes were submitted. Populated mutation/error browser acceptance is not implied by this read-only check.

Combined token checkpoint: **12 tests / 6 files pass** across UI, scoped API reads, mint, revocation, pagination and migration. Inspected mint regression verifies simultaneous legacy/workspace creation shares the five-token cap, viewer and expired-entitlement rejection, one audit event per successful creation and no fabricated installation for independent workspaces. This establishes those local token invariants; live-browser acceptance and full Gate B closure remain separate.

Token 401/403 mutation responses clear stale management controls, any displayed secret and confirmation/name input. Explicit access denial does not enter the network-uncertain creation flow; reloading obtains current permissions. Token UI6, typecheck and diff pass. Broader token mint/revocation concurrency and live-browser acceptance remain separately tracked.

Token creation rejects a successful HTTP response with a missing/empty secret instead of announcing creation. It enters the existing uncertain-outcome flow, requiring history review before another mint attempt. Token UI5, typecheck and diff pass; no real token was minted during rendered testing. Full893 predates this correction.

Combined notification checkpoint: UI, API, save, transport, outbox and workspace suites pass **12 tests / 6 files**. API assertions inspected cover anonymous/foreign/viewer/foreign-origin rejection, redacted reads, unconfigured-provider failure, test idempotency/rate limits, exactly one notification job/audit event and no invented alerts or scan jobs, plus exact disconnect confirmation. Transport tests use controlled doubles; no real provider delivery is claimed. Populated live-browser and hosted delivery acceptance remain distinct.

Notification destination type/value are disabled during a pending mutation, preventing edits from being silently cleared by an earlier save completion. Service failure restores editing and retains the submitted value for correction/retry; authorization failure retains the separate clear-and-refresh behaviour. Notification UI5, typecheck and diff pass; no external send occurred.

Notification mutations rejected for authentication/authorization now invalidate the displayed authority, clear destination input/disconnect confirmation and reload current permissions. Rendered forbidden-save regression verifies read-only controls, cleared sensitive input and no false success; notification UI4, typecheck and diff pass. All requests are test doubles; no real notification was sent. Full893 predates this change.

Current exception/policy integration checkpoint: **19 tests / 7 files pass** across independent approval, connected upload scope, legacy authority, snapshot timing and the rendered request/review flows. Inspected real-store acceptance proves self-approval denial, viewer/foreign-origin denial, exact artifact/path scope, concurrent decision single-winner behaviour, expiry, revoked reviewer denial, archive admission denial and preservation of the original signed evidence after later approval/revocation. B5 API/state evidence is established for those tested paths; populated mixed-source live-browser acceptance remains open. Full893 predates recent UI changes.

Selected exceptions now have their own component scope within a workspace. Changing the selected request aborts obsolete reads/decision feedback and resets its explanation, preventing an old completion notice from appearing against a new request. Rendered same-workspace pending-decision switch regression passes; exception UI11, typecheck and diff pass. Server decisions already admitted may still complete; UI abort does not promise rollback.

Exception error recovery separates history-list failures from selected detail/decision errors. Successful detail refresh clears its stale error while retaining an independent list failure and avoiding a false endless list loader. New rendered recovery test plus existing exception matrix pass10; typecheck and diff pass. Full893 predates these settings fixes.

Exception lifecycle UI acceptance now covers expired, rejected and revoked records retaining reason/history without decision inputs, and approved records offering only explained revocation. Rendered exceptions9 pass; no mutation is sent while displaying terminal states. This expands B5 state evidence without claiming all connected/mixed-workspace browser journeys complete.

Exception workspace-switch acceptance: an outstanding decision request is aborted when its workspace component unmounts; a late successful response does not display success or approval controls in the replacement workspace. Rendered regression verifies new workspace evidence remains selected and read-only; exceptions UI5 and diff checks pass. Abort is not a claim that an already-admitted server decision was rolled back.

Exception polling failure now observes the same mutation-generation guard as successful detail reads. A delayed pre-decision error cannot replace the current decision rejection message. Timed rendered regression exercises the overlapping 30-second poll and rejected mutation; exceptions UI4 and typecheck pass. Source unchanged during the focused run; full893 predates this follow-up.

Exception decision failure now removes stale detail/decision controls and clears old success notices before each attempt. Detail reads begun before a mutation cannot restore their stale successful payload afterward. New permission-rejection UI regression failed before the fix and passes afterward; exceptions UI3, typecheck and diff checks pass. In-flight failure/selection races and broader approval acceptance remain open; full893 predates this fix.

Anonymous proof verification integration now covers passing, failed-policy, artifact mismatch, authentic inconclusive and malformed input in one real-store flow. Jobs, saved scan receipts and hosted usage remain zero after all requests. Targeted regression passes1 (18 deliberately skipped). This closes the tested verification endpoint's no-scan/no-charge outcome cases, not all B6 sharing/revocation/browser acceptance.

Server verification side-effect regression now snapshots jobs, saved scan receipts and hosted heavy-job usage before/after verifying an existing valid proof. All remain unchanged, and verification succeeds despite the separate scan rate limit being exhausted. Targeted real-PGlite test passes1 (18 deliberately skipped); an initial test query used the wrong receipt table name and was corrected to `scan_receipts`. Other proof failure/outcome paths remain separately tested, not covered by this snapshot assertion.

Receipt input failure acceptance: invalid JSON, arrays and null are rejected without a request; a subsequent rate-limited verification removes a previous passing headline. Rendered tests also assert these verification requests use only the receipt verification endpoint, not scan admission. Receipt input8 and diff checks pass. This does not assert backend job side-effect absence, which requires server-level evidence.

Proof outcome presentation regression explicitly covers authentic inconclusive, failed-policy, contradictory receiptOk=false, missing status and passing records. Only passing evidence receives the passing headline. Receipt-input/release-brief/public-verification UI16 pass, including existing scoped verification navigation and late-request isolation checks. This is presentation evidence, not completion of every proof type or backend side-effect invariant.

First Proof evidence wording now explains that signed records preserve outcomes including findings; a signature is not a passing verdict. This removes the earlier implication that only clean checks produce records. First Proof/Overview18 and diff checks pass. React checklist review kept this scoped to existing presentation and event-driven navigation; no design/framework change or full acceptance claim.

Navigation cleanup: leaving Releases now removes the actual `uploadBefore` pagination cursor as well as legacy cursor/detail parameters, preventing an old history page from leaking into subsequent navigation. First Proof GitHub entry uses the shared cleanup helper while explicitly choosing GitHub mode. Route/Overview50 and typecheck pass; full893 predates this correction.

First Proof scan-mode correction: “Scan a package instead” explicitly selects package mode rather than inheriting `mode=github` from the current URL. Workspace scope and the authenticated scan shell remain intact. First Proof/Overview rendered checks pass18 and typecheck passes. Full893 predates this small routing correction; remaining intermediate-state acceptance stays open.

**Latest full result: 893 tests / 189 files passed**, run69440, 236.89 seconds, with application/test source held unchanged. Build73276 passes (existing 506.76kB main-chunk warning); lint exits successfully with existing warnings. This supersedes full881 and all older "latest" checkpoints below for the current monitoring controls, connected Overview and release-selection changes. Full Gate B acceptance is still open: passing regression does not replace the remaining source/state/permission/browser matrices or publication consistency review.

Release selection regressions now exercise two saved attempts: selecting the second updates the scoped URL and preview, restoring the first URL restores its preview, and a late response from an obsolete selection cannot overwrite the current result. Rendered release suite passes **12 tests**; diff check passes. This is component/URL evidence, not populated multi-release live-browser acceptance or full Gate B completion.

Release filter/deep-link clarity: a selected saved attempt outside the active status filter now explains why its evidence remains visible while the filtered list excludes it. Rendered regression verifies a failed deep link under the passed filter; release UI10, typecheck and diff checks pass. Removed an unused invalid test prop; workspace remains URL-derived. This does not close the remaining complete release-state/browser matrix.

Live local browser acceptance (8 September): existing workspace artifact history opens its exact saved release brief. Selecting the second recorded finding updates `uploadFinding=1`; reloading preserves the workspace, upload, detail mode, selected MAP-001 finding and artifact-only scope. Private sharing remains an explicit unpublished summary with separate verification/download explanations. No scan, publication or external provider action was triggered. This proves the existing artifact finding/reload path only; multi-release, connected-source and broader B4/B6 matrices remain open.

Monitoring controls refresh races: parent inventory refresh no longer clears the saving/disabled state of an outstanding mutation. Mutation completion invalidates outstanding inventory reads, preventing a late successful read from restoring stale controls after a permission rejection. Five rendered monitoring-control regressions pass, including both races; typecheck and diff checks pass. Full881 predates this change; broader permission/lifecycle acceptance remains open.

Overview follow-up: unknown, delayed or suspended connected monitoring now directs the next action to workspace Coverage even when saved releases passed. Open alerts and active scans retain priority. Expanded server regression includes two connections in one workspace (one suspended) and excludes a foreign workspace. Focused next-action/UI/server checks pass **22 tests / 3 files**, and typecheck passes. This is not full Gate B acceptance; full881 predates these Overview changes and populated browser acceptance remains open.

Connected monitoring now provides workspace-scoped, de-duplicated connection identities and per-connection Coverage links. UI regression verifies a second connection opens with its explicit installation ID; server/UI14 and expanded UI13 pass, typecheck passes. Populated multi-connection browser acceptance remains open; full881 predates this addition.

Connected Overview freshness regression now explicitly covers recent, delayed, future-dated and unavailable cadence inputs. Future timestamps/unknown cadence remain unknown; no release verdict is used to infer freshness. Focused Overview14 and typecheck pass. Multi-installation drill-down and complete browser acceptance remain open.

Connected monitoring aggregation includes installation-backed websites using their configured hourly-minimum cadence, separately from independent workspace websites. Pending ownership verification forces unknown freshness even when a timestamp exists. Server regression covers unknown→verified recent→disconnected and passes; typecheck passes. UI description now includes websites. Full881 predates these Overview changes.

Connected monitoring summary excludes disconnected installations and marks suspended installations unavailable rather than recent. Rendered tests cover the warning on both First Proof and daily Overview without changing release totals; server regression excludes a foreign workspace's package. Focused checks14 pass, expanded server check passes and typecheck passes. Full881 predates Overview additions; connected website coverage remains separate pending parity.

Overview now exposes a workspace-authorized connected monitoring summary using configured cadence, separately from release decisions, with a Coverage link. Repository/package/custody sources are included, disconnected sources excluded, paused sources distinct; unknown timestamps remain unknown. Typecheck and focused server/UI checks pass, including separation from passed totals. Full881 predates this addition. Installation suspension, connected website parity and populated rendered acceptance still require verification.

**Latest full result:** run79406 passed **881 tests / 189 files** in 239.10 seconds against unchanged application/test source. This supersedes full873 for the lifecycle changes recorded here. Build65166 passed on this source; lint had warnings but no errors. This is regression evidence, not closure of all B0–B8: remaining complete-journey acceptance, connected Overview health and publication consistency must still be addressed.

Latest validation before the next full run: build65166 and diff checks pass. Lint passes with warnings (including SourceMonitoringControls synchronous effect state reset); no lint errors. Full873 predates later lifecycle work and is not current full acceptance. A fresh full regression is being started with source held unchanged.

Candidate observation persistence now locks and validates its parent source generation for both missing-name checks and registered candidate updates. Direct stale-generation regression confirms neither advances observation time. Identity19 passes; the expanded selected regressions pass2; typecheck passes. These are source-lifecycle guards, not proof of complete multi-record publication/notification atomicity.

Lookalike processing now checks source generation before each registry request and after its response, stopping the batch on lifecycle change. Two selected regressions verify failed requests save no observations and a generation change stops after the first request without saving its result. Existing identity18 and typecheck passed before expanding that regression. Conditional observation writes remain separate from the before/after checks.

Lookalike registry request failures no longer become completed candidate observations. A failed request leaves its previous observation timestamp intact instead of treating failure as an unregistered-name result. New real-store regression confirms requests occurred and timestamps remain unset; identity18 and typecheck pass. Full873 predates this correction.

Multi-entry import regression now proves a source conflict returns its row while the following valid package is protected (2 selected tests pass). Candidate creation captures current source generation and inserts its batch under a guarded source lock; inactive sources create none. Identity17 and typecheck pass. Candidate observation/update races are distinct and not closed by candidate creation guarding.

Bulk import lifecycle conflicts now produce a per-package `connection_changed` result rather than discarding the response for previously processed entries. UI labels instruct refresh/retry and current watched identity is read for the affected entry. Single/bulk ownership-race tests pass (2 selected), and typecheck passes. Other unexpected import failures and multi-entry rendered feedback still need acceptance.

Bulk protection now captures existing-source generation before ownership lookup and passes it into guarded protection/snapshot persistence; paused existing sources are rejected. Single/bulk ownership-race regressions both pass, existing identity17 passes and typecheck passes. Bulk import remains incremental rather than all-or-nothing; candidate persistence and partial-result UX need further acceptance.

Targeted protection-setup race regression passes: registry ownership lookup changes source generation before returning; setup rejects and neither protection nor identity snapshot is stored. One selected test passes (20 deliberately skipped by filter), and typecheck passes. This directly verifies the preceding single-package setup guard; bulk setup remains unverified.

Single-package protection setup now captures source generation before registry work, rejects inactive sources and locks/revalidates generation before saving protection. It passes the original generation into identity synchronization. Typecheck passes; package-identity regression verifies existing setup compatibility. Bulk protection and candidate persistence remain separate outstanding lifecycle review paths.

Protected-identity synchronization now captures an active generation even for direct callers, stops for paused/disconnected sources, and passes that generation to snapshot and alert persistence. Existing npm/identity37, typecheck and diff checks pass. This protects synchronization from its entry point; upstream setup fetch/authorization races and identity candidates still require their own review.

Polling identity snapshot persistence now validates/locks the expected package generation inside its transaction. Late snapshots from a reconnected source are rejected before insertion. Npm/identity37 plus lifecycle1 pass and typecheck passes. Direct protection-setup callers without a captured generation and identity candidate writes remain outside this new guard; no whole-pipeline atomicity claim is made.

Package polling now supplies a generation-bound notifier to protected-identity and identity-signal processing, including the no-external-notifier fallback. Those alerts therefore use the guarded persistence path instead of dropping source generation. Existing npm/identity suites pass37 and typecheck passes. Identity snapshot writes and their race-specific acceptance are not claimed complete; this follows full873.

Registry metadata updates now accept an expected connection generation and reject stale writes in SQL. Active package checks pass it and stop if persistence rejects; missing-package alerts also use the guarded generation. Regression proves a stale version cannot overwrite a reconnected package. Npm/lifecycle21 and typecheck pass. Identity-signal pipelines and cross-record atomicity remain open; full873 predates this change.

Package check admission now rejects paused/disconnected sources before registry access and rechecks the captured generation after registry completion before processing metadata or missing-package outcomes. Two real-store regressions prove no registry fetch for paused/disconnected source objects. Npm suite20 and typecheck pass. Conditional metadata persistence and identity-signal publication races remain distinct from these admission checks.

Package monitoring freshness correction: unavailable entitlement, missing registry credentials and registry request failures no longer advance `last_checked_at`. A failed request is not a successful observation. Added real-store failure regression; npm suite18 and typecheck pass. Error-detail presentation and the broader freshness matrix remain separate acceptance items; full873 predates this fix.

Saved independent release browser acceptance: opened existing sourcemap.tgz from the live ledger preview into its full brief, then reloaded. Exact upload/workspace/detail selection survives reload; three recorded findings, artifact-only scope, checked timestamp and private sharing preview remain visible. Publishing was not invoked. This proves this saved artifact journey, not multi-release selection, mixed-source or public proof outcomes.

Local rendered evidence (8 September): `/watch/sources` loads the real independent local-review workspace, shows its empty website inventory and separates Coverage from saved Releases. Clicking global New scan retains workspace and product shell. GitHub and Production website tabs update the URL and selected panel; website continuation is disabled without input and explains ownership verification. No provider connection, scan or customer mutation was performed. This verifies empty independent navigation only, not populated connected monitoring controls or hosted execution.

Coverage child inventories now reload when the parent source inventory changes, preventing monitoring/retained panels from remaining stale after source changes in the same screen. Three rendered monitoring tests pass, including parent refresh; typecheck passes. This is component evidence, not a completed browser journey, and follows full873.

Further source-scope correction: new custody identity aggregation excludes disconnected packages and websites while preserving their stored historical identity. Pausing alone does not delete a connected source's saved identity or its evidence scope. Lifecycle reconnect regression and custody suite pass (12 tests / 2 files); typecheck passes. Full873 predates this correction.

Subsequent change (not covered by full873): custody alerts now carry their configuration version and validate it under a source row lock before persistence/delivery admission. Typecheck passes; lifecycle regression verifies stale credentials/configuration cannot publish an alert. The full checkpoint below predates this addition.

Full `npm test` run41111 completed successfully against unchanged application/test source: **873 tests / 189 files passed**, 242.30 seconds. Production build18204 also passed; the existing 506.76 kB main-bundle warning remains. This supersedes earlier local-verification availability statements for this checkout, not the historical PR environment or remaining acceptance requirements. The run includes retained package/custody lifecycle, monitoring HTTP/UI, generation-bound npm admission and guarded receipt/metadata/alert writes. No deployment or customer provider activation occurred.

Remaining source-publication work is explicitly bounded: package writes are individually guarded but not one complete publication transaction; custody alert publication has not yet been given an equivalent configuration guard; delivery-after-commit behaviour needs a defined lifecycle contract. Populated browser/mixed-workspace acceptance and other B0–B8 ledger items remain open. The passing suite is not full Gate B acceptance.

## Monitoring HTTP acceptance correction — 8 September 2026

Real application-route regression caught `install` versus `installationId` query mismatch in monitoring/retained-source UI, also present in the older DisconnectedRepositories component. Corrected all three callers. HTTP/storage regression now passes anonymous/viewer/foreign/cross-origin rejection, invalid payload rejection, owner pause/resume, duplicate audit suppression, archive denial, disconnected mutation denial and revoked retained-read isolation. The read endpoints intentionally return empty source lists for inaccessible connections. Browser acceptance is still required; this result demonstrates Hono/API plus real database behaviour, not rendered clicking. No customer data or external providers were changed.

## Monitoring audit transaction — 8 September 2026

Migration 119 extends the existing audit action allowlist for source pause/resume. Monitoring changes now lock the source, check effective management authority and commit state/generation plus actor-attributed audit history together. Repeating the same state is idempotent and does not create a second event or invalidate another check. Existing Audit screens can read these ordinary connection-scoped events. Typecheck and initial database regression pass; extended duplicate/event-retention migration assertions run separately. Browser/HTTP acceptance and concurrent membership-revocation fencing remain outstanding; no broad completion claim.

## Source pause/resume — 8 September 2026

Migration 118 adds package/custody pause state and a custody connection generation. Coverage has source management controls backed by scoped list/mutation APIs. Effective workspace roles govern mutations (package member/admin, custody admin; archived projects read-only); polling omits paused sources, package generation checks reject paused work, and custody snapshots/completion include the generation so pause/resume invalidates earlier checks. Resume clears check freshness. No new scan is implied by toggling a control.

Typecheck and focused lifecycle/source regressions are being collected for this snapshot. Still required: lifecycle audit events, mutation/refetch/browser acceptance, full HTTP negative matrix, reconnect pause-state semantics and atomic publication beyond the current guards. These controls are implementation progress, not B3 completion. Broader B0–B8 requirements remain in the ledger and existing screens must not be rebuilt.

## Package connection generation — 8 September 2026

Migration 117 adds a package connection generation incremented on disconnect/reconnect. Workers capture it before fetching and check it after download, after scanning and before handling scan errors. Work from an older connection stops instead of refreshing the reconnected source; downloads abandoned before parsing release the unused reservation. Scope lookup rejects another installation. Twenty-eight package/usage/removal regressions and typecheck pass; extended generation assertions are verified separately. This is pre-publication revalidation, not atomic fencing across all subsequent receipt/notification writes; that remaining race and stale jobs queued before a reconnect still require completion. Pause/resume and lifecycle event UI remain open. No gate is closed by this slice.

## Custody in-flight configuration guard — 8 September 2026

Worker custody lookup now reads the destination and encrypted credential generation in one snapshot. Result updates conditionally require the same encrypted credential generation and a connected destination; rejected stale completions return before alert publication. Configuration replacement already rotates encrypted ciphertext and clears current check state. The generation value is internal and never returned through customer APIs. Database regression checks disconnected completion rejection, reconnect-with-new-credentials rejection of the old completion, and acceptance of the new check. Twelve custody/removal regressions and typecheck passed before the final extended assertions; the dedicated extended regression is recorded by the continuation session. This guards completion storage, not a claim of full linearizability of every later notification side effect. Package in-flight publication and full pause/resume remain open.

## Retained inventory and reconnect freshness — 8 September 2026

Coverage now mounts a scoped retained-package/custody list with reconnect configuration links. Its no-store API uses effective workspace membership and returns only source labels, identifiers and disconnect times, never credentials. Database checks prove foreign users/connections see no retained records and reconnect removes rows from retained inventory while preserving IDs. Package reconnect resets metadata freshness; custody credential/configuration replacement clears previous check status/fingerprint so old verification cannot describe new credentials. Saved release evidence remains separate.

Typecheck, 12 custody/removal tests and a fresh extended retained-list database test pass. Production build verification follows. Still open: explicit pause/resume, recorded lifecycle event history, stale in-flight completion fencing across disconnect/reconnect, populated browser/HTTP acceptance and refreshed inventory after mutations. Reconnect links currently open the existing scoped configuration forms; they do not bypass permissions or silently restore credentials.

## Retained source configuration — 8 September 2026

Migration 116 adds disconnection timestamps to watched packages and custody destinations. Removal retains rows and associated history; custody disconnection clears encrypted credentials. Active readers/polling exclude disconnected rows, reconnect reuses the original ID, and queued npm scans revalidate package identity/scope before downloading. Workspace alert source counts exclude disconnected packages. Existing typed confirmation and effective workspace role checks remain in force.

Fresh verification: 39 tests across removal-access, npm-watch, map-custody and usage suites plus typecheck pass. These prove the current retained configuration and ordinary source regression slice, not full lifecycle completion. No production migration or customer removal was performed. Still required: visible retained inventory, pause/resume controls, lifecycle event history, race checks during in-flight outbound work/reconnect, stale-check reset semantics and rendered acceptance. The source-count predicate was added after that focused run and needs its own corresponding verification.

## Source mutation authority — 8 September 2026

Reviewed checkpoint `be3f408` is committed locally and pushed to `codex/v20-homepage-auth-mock3`; remote `backup89` remains `eb46118`. Latest checkpoint checks: 47 focused tests and production build/typecheck passed. The interrupted later full run has no recovered terminal result; do not replace that with a claimed full pass.

Continued B0/B3 work fixes package and custody removal queries that still joined legacy installation membership. They now use effective workspace membership, with member/admin permission for packages and admin-only custody removal; archived membership projects read-only. A real database regression covers explicit administrators without legacy rows, viewer/member custody denial, archived/revoked/foreign denial and allowed removal. Three tests across two suites plus typecheck pass. These changes are subsequent local work, not included in `be3f408`.

Concrete remaining B3 implementation: package and custody configuration removal still physically deletes configuration rows. Full retained lifecycle, pause/reconnect, worker fencing and corresponding UI remain to implement and verify; the authority correction does not close those requirements or authorise deletion of customer evidence. No customer configuration was removed during this test (in-memory fixtures only).

## Local integration and continuation — 8 September 2026

Verification recovery: the subsequent full-run session 9422 and its wrapper became unavailable after conversation interruptions; its terminal result was not recovered and must not be called a pass. The earlier completed 862-test PR run below remains valid for that snapshot. The later Coverage correction has separate focused/build evidence. Owner clarified that all B0–B8 have substantial implementation: Open rows represent remaining gaps/acceptance, not instructions to rebuild completed sections.

PR #41 (`a50ea7007efbc3660aace91131cf4ff4784fbecd`) was reviewed in a separate checkout and fast-forwarded into the local working branch after **862/862 tests across 185 files passed** (242.34 seconds), together with semantic typecheck and production build. `backup89` remains at `eb46118`. No remote merge or deployment was performed.

The GitHub CI failure is now explained: check-run 102022502098 reports that the job did not start because recent account payments failed or the Actions spending limit needs increasing. This is an account-side blocker; local test success does not change that remote check result.

Subsequent B3 correction: Coverage now selects release evidence by the scanner's exact typed source coordinate instead of partial name matching. Similar package/repository names and different source kinds cannot supply another source's latest-release link; website URLs preserve protocol/path distinctions; latest selection is timestamp-based and leaves history order intact. Eight focused tests across three files and the production build/typecheck pass. The 862-test result predates this correction and is not a full-suite result for the later source.

Local review server restarted at `http://127.0.0.1:4347` with role `web` (background jobs disabled). Browser sign-in successfully reopened the existing personal workspace and real saved scan evidence. This verifies local login/Overview only, not connected Coverage lifecycle acceptance. Remaining work is still the B0–B8 ledger below; next is connected-source lifecycle parity and rendered Coverage/selected-release navigation.

Owner's latest authorisation permits local integration and continued Gate B implementation; the review-branch-only publication boundary recorded below describes the earlier ChatGPT handoff, not a prohibition on this authorised local integration.

## Current checkpoint: 8 September 2026

**Gate B remains incomplete. All B0-B8 acceptance rows remain open.** This checkpoint implements bounded B1/B3 evidence-presentation corrections, not the entire continuation prompt or launch acceptance.

Source branch: `codex/v20-homepage-auth-mock3` at `eb46118030643a8f27847302784867df4e84c6da`.
Review branch: `codex/gate-b-evidence-integrity-20260908`.

The complete previous audit is preserved without content changes in [GATE-B-COMPLETION-AUDIT-2026-09-06.md](GATE-B-COMPLETION-AUDIT-2026-09-06.md). It contains the historical checkpoint chronology, implementation references and earlier browser evidence. Its top checkpoint recorded full run 83464 as 806/814, with targeted follow-ups, not a clean final acceptance run. Historical local process IDs and test reports have not been reproduced in this environment.

### Implemented in this checkpoint

- Coverage no longer treats an empty package-version coordinate as a wildcard matching every unrelated alert. Missing/whitespace coordinates are excluded; acknowledged findings remain open, resolved findings are excluded and a matching alert is counted once.
- Server-provided unknown or unrecognised freshness can no longer leave an otherwise successful source green. This applies to GitHub, npm, websites and private map custody. Critical findings and warning/error/pending states remain visible. Delayed sources still appear in the attention filter; saved results/timestamps and explicitly unknown next dispatch remain unchanged.
- Setup no longer treats a crawl timestamp alone as successful inspection. Positive inspection requires a valid recorded timestamp and an explicit successful status, or the existing recorded-public-map findings path. Inconclusive, error, running, queued and unrecognised outcomes cannot become completed crawl proof solely because a timestamp exists.
- Map custody uses exact positive statuses (`passed`, `verified`, `match`), not substring matching that accepted `unverified` or `mismatch`. `passed` is emitted by the existing map-custody evaluator. Missing/invalid timestamps and lookup errors cannot verify custody. Recorded setup capability is distinct from a clean release verdict: where public-map evidence remains, setup copy explicitly says exposure still needs action and Coverage stays critical.

The changes stay inside the existing view-model layer and preserve the current screens, routes, API contracts and approved four-circle journey. There is no new feature, authentication provider, migration or customer-data insertion.

### Fresh verification and its limits

`tests/watch-evidence-integrity.test.ts` adds 42 regression cases. The actual view-model and server freshness functions were executed locally under Node 22.16.0 with native TypeScript stripping. For this focused standalone run only, the test-framework import was changed from `vitest` to `node:test`; the assertions and cases were unchanged. Imported runtime helpers were retrieved from the repository, not mocked.

- Original source: **12 passed, 30 failed** across the new 42 cases.
- Patched source: **42 passed, 0 failed** against unchanged application source after the patch.
- TypeScript transpilation syntax checks passed for the changed view model and the new test file. This is not a semantic project typecheck.
- Git blob hashes of the uploaded source and test match the locally executed files.

The environment has no complete local checkout/dependency installation and cannot resolve GitHub/npm network hosts. Consequently **the full Vitest regression, semantic project typecheck, production build and rendered browser/API journeys are not locally verified**. The existing GitHub CI workflow now also runs `npm run build` after the full test command; build/test results must be read from the PR checks and must not be inferred from the focused run. Do not edit source/tests while that full run is active.

To reproduce with the normal project dependencies: `npm test -- tests/watch-evidence-integrity.test.ts`, then `npm test`, `npm run build`, and the applicable real browser/API acceptance journeys. Do not create external credentials, notifications or customer fixtures to exercise a screen.

### Complete acceptance ledger

The inherited implementation evidence below is historical, not fresh proof for this branch. No row is closed by the focused tests.

| Requirement | Inherited implementation evidence | Remaining proof/work | Status |
| --- | --- | --- | --- |
| B0 identity/workspaces | Workspace membership/billing, fresh GitHub binding, website ownership and stable assignment migrations | Full resource ownership and mixed-source acceptance; provider activation remains deferred | Open |
| B1 First Proof/setup | Four-circle empty state, saved artifact transition; this checkpoint corrects crawl/custody evidence predicates | Full evidence-driven intermediate states, diagnostics and rendered acceptance | Open |
| B2 New Scan | Durable uploads/login claim, GitHub intent/binding, independent website challenge/admission/saved results | Complete source-type, permission, cancellation, retry, expiry, trial and navigation matrix | Open |
| B3 Coverage | Independent website lifecycle/schedule/health, retained disconnection, cadence and ownership fields; this checkpoint corrects alert/freshness presentation | Lifecycle and scheduling parity across source kinds, exact source attribution, successful-check semantics and rendered acceptance | Open |
| B4 Releases/detail | Saved selection, preview/detail, pagination and immutable uploaded evidence | Full source/type/state parity and browser reload/back-forward verification | Open |
| B5 Alerts/exceptions | Website publication, workspace responses/history, stable assignment, pagination and scoped exception/snapshot enforcement | Populated response, approval/denial/expiry and mixed-source browser acceptance | Open |
| B6 Proof | Private records and explicit redacted publication/revocation | Complete outcome taxonomy and upload races across proof types, no scan-job side effects and rendered verification | Open |
| B7 Daily Overview | Saved-scan totals, website health/filter links and mixed-workspace First Proof transition | Correct connected aggregation and evidence-driven intermediate states with browser acceptance | Open |
| B8 Settings | Team/policy/retention/audit; independent tokens/notifications and connected APIs | Rendered save/error/audit and permission acceptance across workspace types and lifecycle boundaries | Open |
| Cross-cutting acceptance | Historical suites in the preserved audit; 42 fresh focused regression cases in this checkpoint | Fresh full regression/build plus actual API/browser, isolation, access-revocation and failure-state matrix | Open |

Known limits of this slice: legacy source-coordinate matching still uses non-empty substring fallback rather than authoritative source IDs; omitted monitoring metadata retains existing compatibility behaviour; setup capability evidence is not whole-workspace safety or source-attributed custody proof. These must not be represented as complete tenant-isolation or current-clean acceptance.

### Next authorised work and owner boundaries

Resume the original Coverage lifecycle/scheduling and rendered cadence checkpoint, then the complete matrix in [CONTINUE-GATE-B-PROMPT.md](CONTINUE-GATE-B-PROMPT.md) and [ENTERPRISE-READINESS-PLAN.md](ENTERPRISE-READINESS-PLAN.md). Revalidate against actual code; the preserved audit's historical defects are not automatically still current.

Only this new review branch and a draft PR are authorised for publication. Do not push to `main`, rewrite the source branch, merge, deploy, migrate production, install Docker, activate providers, create paid resources or send external notifications. `vercel.json` disables automatic deployment for this review branch only; other branches' settings are unchanged. CI uses read-only repository permissions and tests/builds local fixtures, not live deployment.

Gate A hosted isolation, Gate C broader visual/accessibility work, Gate D enterprise capabilities and Gate E operational/commercial readiness remain separately deferred as already agreed. Disconnect retains history; destructive customer-data deletion still requires the lifecycle policy's explicit typed authorisation. A passing focused suite, or even passing CI, is not a whole-application launch declaration.
# Monitoring controls UI verification — 8 September 2026

Npm notifier inputs now carry the source generation for normal, oversized and missing-registry-token alerts. Alert persistence locks and validates that generation transactionally before inserting alert/release links; rejection prevents notifier delivery from starting. The lifecycle regression verifies stale alerts are not inserted; npm/lifecycle suites pass 18 tests / 2 files. Notification delivery after a successfully committed alert and cross-record atomicity remain distinct open checks.

Package scan metadata and map identity updates now conditionally write only against the worker's expected connected, unpaused generation, returning acceptance to stop subsequent work when rejected. Direct stale-generation regressions cover both updates. After correcting an initial patch-target error, typecheck and the npm/lifecycle suites pass 18 tests / 2 files. This protects individual writes; alert notification and cross-write atomicity are still open.

Receipt persistence now checks and locks the expected package generation inside its transaction before writing receipt/evidence/revision records. Npm workers pass that generation for normal and oversized results. Regression rejects stale generation with no receipt saved and permits the current generation. Npm-watch17 passes; receipts19 passes with CLI subprocess permissions (restricted combined run had two CLI failures); typecheck passes. Package metadata and notifier publication still occur separately and require further work; this is not whole-publication atomicity.

The real npm worker regression now also changes source generation during scanning: downloaded input is scanned, but no alert or package last-scanned update is published. The suite passes 17 tests; typecheck passes. Inspection confirms remaining publication writes span separate receipt, package, identity and notifier operations, so this evidence must not be described as transactionally atomic publication.

Worker admission regression: a real queued npm job whose source generation changes before execution finishes without downloading a tarball or creating an alert. The expanded npm-watch suite passes 16 tests and typecheck passes. This verifies pre-execution stale-job rejection; concurrent publication remains separately unproven.

Queued npm checks now carry their source connection generation, and deduplication includes it so a later reconnection can queue a fresh check. Worker admission rejects jobs from earlier generations; legacy jobs without this field are accepted only on unchanged generation zero. The npm-watch suite verifies generation tagging. This is admission protection, not atomic publication protection; races after the final worker check remain open.

Reconnect semantics verified: an explicit package/custody pause survives disconnect and reconnect; neither worker check becomes eligible until an explicit resume. Source IDs are retained. The disconnected-source panel now explains this behaviour. The database lifecycle regression, monitoring HTTP test and rendered controls tests pass together (4 tests / 3 files). This closes the ambiguous reconnect-pause behaviour, not the remaining in-flight publication or complete source acceptance requirements.

Rejected monitoring mutations now remove stale actionable rows and require a fresh inventory read. Refresh clears the error into an explicit loading state; successful saves display server-confirmed state. In-flight mutations are aborted on source-context change/unmount and duplicate submissions are guarded. Two rendered UI regressions plus the real monitoring HTTP acceptance test pass (3 tests / 2 files). These checks do not establish populated browser acceptance or complete Gate B.
