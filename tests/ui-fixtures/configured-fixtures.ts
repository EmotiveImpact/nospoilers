// Presentation-only fixtures derived from the existing component tests.
export function configuredFixture(mode: 'production'|'explanation') {
 if(mode==='production')return {canManage:true,canCancel:true,baselineRevision:2,manifest:[{path:'dist/assets/long-production-application-bundle.js',size:2048}],origins:[{id:12,origin_url:'https://production-fixture.example/',eligible:true,eligibility:'eligible'}],runs:[],notice:'Fixture only. No observation can be submitted.'};
 return {scope:{workspaceId:'fixture',streamId:'fixture',snapshotId:'fixture'},consentKey:'fixture',available:true,providerId:'fixture-provider',canRequest:true,canReview:true,budget:{currency:'USD',maxCostMinor:1,dailyCostMinor:10,reservedCostMinor:0,notice:'Ceilings, not charges.'},projection:{schemaVersion:1,scanStatus:'fail_policy',readiness:'attention',held:false,files:12,totalBytes:1000,findings:2,suppressed:0,severityCounts:{critical:1,high:1,medium:0,low:0,info:0,unknown:0}},items:[{id:'fixture-output',snapshot_id:'fixture',state:'pending',text:'Fixture only: review the original signed findings before making a decision.',reviewed_text:null,created_at:'2026-09-09T00:00:00Z'}],remaining:10,notice:'Fixture only. No provider is connected.'};
}
