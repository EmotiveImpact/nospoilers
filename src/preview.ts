/** Layout-only desk used when GitHub is not connected. It never invents tenant rows. */
export const PREVIEW_LOGIN = "Preview"

export function previewRepos(): [] {
  return []
}

export function previewAlerts(): [] {
  return []
}

